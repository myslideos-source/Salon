> **Hinweis:** Dieser Umsetzungsplan basiert auf `docs/HALLOMIA_UNIVERSAL_KONZEPT.md` und dokumentiert den Ist-Zustand des Projekts sowie einen phasenweisen Weg dorthin. Er wurde rein analytisch erstellt — in diesem Schritt wurden kein Anwendungscode und keine Datenbankstruktur verändert. Build, TypeScript-Prüfung und Lint wurden ausschließlich diagnostisch ausgeführt.

## 1. Aktueller technischer Stand

### 1.1 Tech-Stack & Paketmanager

- **Framework:** Next.js 16.3.3 (App Router, Turbopack), React 19.2.8, TypeScript 5 (strict)
- **Styling:** Tailwind CSS v4 (`@tailwindcss/postcss`), eigenes Design-System über CSS-Variablen (`src/app/globals.css`), Icons via `lucide-react`
- **Backend/DB:** Supabase (Postgres, Auth, Realtime, Storage), Zugriff über `@supabase/ssr` und `@supabase/supabase-js`
- **KI/Telefonie:** OpenAI (`openai`, nur für den In-App-Testanruf), Retell (`src/lib/voice/providers/retell.ts`) und ElevenLabs (`src/lib/voice/providers/elevenlabs.ts`) als austauschbare Voice-Provider hinter `src/lib/voice/provider.ts`
- **Validierung:** Zod
- **Datum/Zeit:** `date-fns`, `date-fns-tz`
- **Tests:** Vitest + Testing Library + jsdom (`tests/availability.test.ts` — reine Scheduling-Logik)
- **Paketmanager:** **npm** (nur `package-lock.json` vorhanden, kein yarn.lock/pnpm-lock.yaml)
- **Scripts:** `npm run dev|build|start|lint|test`, `npm run seed` (führt `scripts/seed.ts` aus)

### 1.2 Verzeichnisstruktur (Kurzüberblick)

```
src/app/            Next.js App Router: Landingpage, /admin/*, /app/*, /api/*
src/components/      admin/ · appointments/ · auth/ · brand/ · calendar/ ·
                     calls/ · customers/ · dashboard/ · layout/ · marketing/ ·
                     portal/ · ui/
src/lib/             actions/ (Server Actions) · auth/ · scheduling/ ·
                     sms/ · supabase/ · validation/ · voice/
supabase/            migrations/0001–0013 · seed.sql · tests/rls_and_booking.sql
tests/               availability.test.ts
public/brand/        Logo-Assets (bleiben laut Vorgabe erhalten)
```

### 1.3 Seiten, Routen und Navigation

Es gibt **zwei getrennte Portale** mit eigenem Login und eigener Navigation:

- **`/admin/*`** — Plattform-Admin (HalloMia-Team, „Managed Service"): `dashboard`, `salons`, `salons/[salonId]/{calendar,employees,services,opening-hours,working-hours,customers,calls,ai,settings}`, `leads` (Akquise-Pipeline `sales_leads`), `signups`, `system`.
- **`/app/*`** — Salon-Portal (Kunde/Salon-Mitarbeiter): `dashboard`, `calendar`, `appointments`, `customers/[customerId]`, `calls`, `absences`, `ai`.
- Öffentlich: `/` (Landingpage), `/impressum`, `/datenschutz`.
- API: `/api/voice/webhook` (Retell), `/api/voice/webhook/elevenlabs/{[tool],post-call}`, `/api/calendar/feed.ics` (ICS-Export), `/api/notifications`, `/api/cron/resync-voice-agents`.

Navigation ist in `src/components/layout/sidebar.tsx` (Desktop) und `mobile-nav.tsx` (mobiles Bottom-Nav, horizontal scrollbar, **kein „Mehr"-Menü**, sondern alle Punkte in einer scrollbaren Leiste) implementiert. Die Nav-Items selbst sind bereits generisch benannt (`Übersicht`, `Kalender`, `Termine`, `Kunden`, `Anrufe`, `Abwesenheiten`, `KI-Assistent` in `src/app/app/(portal)/layout.tsx:13-21`); es fehlen aber die im Konzept geforderten Punkte **Anfragen**, **Leistungen** (im Salon-Portal, aktuell nur im Admin-Portal sichtbar) und **Team und Ressourcen** (aktuell nur „Abwesenheiten", kein Mitarbeiter-Self-Service).

### 1.4 Wiederverwendbare Komponenten

Bereits gut vorbereitet und branchenneutral nutzbar:

- `src/components/ui/*` (Button, Card, Badge, Field, Modal, PageLoading) — generisches Design-System, keine Fachlogik.
- `src/components/calendar/*` — **sehr weit entwickelter eigener Kalender**, kein eingebetteter externer Kalender: `calendar-shell.tsx` orchestriert `day-view`, `week-grid`, `month-grid`, `mobile-agenda` + `mobile-date-strip` (echte mobile Tagesansicht mit horizontaler Datumsleiste, nicht die verkleinerte Desktop-Woche — Konzeptanforderung ist hier **bereits erfüllt**), `mini-month-calendar`, `today-appointments-card`, `week-stats-card`, `employee-filter`, `appointment-card`, `appointment-detail-modal`, `new-appointment-modal`. Vorhanden: Tag/Woche/Monat, Heute-Button, Vor/Zurück, aktuelle-Zeit-Linie, Drag-to-Reschedule (Pointer-Events), Farbcodierung nach Leistung/Mitarbeiter, Mitarbeiter-Filter, Realtime-Update via Supabase-Channel mit Ton/Push-Benachrichtigung.
  **Fehlend gegenüber Konzept:** Suche, Filter nach Standort/Terminart/Status, Terminübersicht als reine Liste über den Kalender hinaus (existiert separat unter `/app/appointments` via `appointments-list-view.tsx`, aber nicht als Kalender-Ansicht), Verlängern/Verkürzen eines Termins per Drag (nur Verschieben ist implementiert, keine Resize-Handles).
- `src/components/customers/*`, `src/components/calls/*`, `src/components/appointments/*` — CRUD-Views, strukturell generisch.
- `src/components/admin/*` — Formulare für Mitarbeiter, Leistungen, Öffnungszeiten, Arbeitszeiten, Abwesenheiten, Voice-Settings; funktional bereits recht flexibel (Leistungen haben u. a. Dauer, Preis, Vor-/Nachbereitungszeit, Farbe, Mitarbeiterzuordnung — deckt einen Großteil der Konzept-Anforderung an „Terminarten" bereits ab).

### 1.5 Datenbank, Migrationen und vorhandene Tabellen

13 sequenzielle Migrationen (`supabase/migrations/0001`–`0013`), additiv gewachsen, keine Down-Migrations. Kern-Tabellen (alle mandantenscoped über `salon_id`, außer `platform_admins`, `sales_leads`, `audit_logs`):

| Tabelle | Zweck | Bemerkung |
|---|---|---|
| `platform_admins` | Admin-Allowlist | user_id → auth.users |
| `salons` | **Mandant** (= „Unternehmen") | 1 Zeile = 1 Standort/Unternehmen, kein separates Standort-Konzept |
| `salon_users` | Mitgliedschaft | Rolle nur `owner` \| `staff` |
| `employees`, `employee_working_hours`, `employee_absences` | Personal | einzige „Ressource"-Art; keine Räume/Fahrzeuge/Geräte |
| `services`, `employee_services` | Leistungen/Terminarten | bereits recht flexibel (Dauer, Preis, Puffer, Farbe, Mitarbeiterzuordnung, aktiv/inaktiv) |
| `customers` | Kunden | nur Basisfelder, kein Status/Tags/individuelle Felder |
| `appointments`, `appointment_services` | Termine | Status nur `booked/completed/cancelled/no_show`, Quelle nur `voice_ai/manual/online_booking`; **DB-Exclusion-Constraint** verhindert Doppelbuchung pro Mitarbeiter |
| `calls`, `callback_requests` | Anrufe & Rückrufe | `callback_requests` ist einfach (kein Kategorie/Dringlichkeit/Anhang-Modell wie im Konzept gefordert) |
| `voice_settings` | KI-Konfiguration | pro Salon, mit Retell/ElevenLabs-Feldern, `custom_prompt`, mehreren Verhaltens-Flags (bereits branchenagnostisch erweitert, siehe 0009–0011) |
| `business_hours` | Öffnungszeiten | pro Wochentag, kein Ausnahme-/Feiertagskalender |
| `sales_leads` | Interne Akquise (Admin-only) | nicht mandantenscoped, gehört zum HalloMia-eigenen Vertrieb, nicht zum Kundenprodukt |
| `audit_logs` | Audit-Trail | vorhanden, aber nirgends im UI ausgewertet |

**Fehlend gegenüber Konzept:** `locations` (Standorte), generische `resources` (Räume/Fahrzeuge/Geräte/Tische), `requests` (branchenunabhängige Anfragen mit Kategorie/Dringlichkeit/Anhang), `faq` als eigene Tabelle (aktuell nur `custom_prompt`-Freitext), individuelle Buchungsfragen pro Terminart, individuelle Kundenfelder, Kunden-Tags/-Status, `notifications`-Tabelle (Benachrichtigungen laufen aktuell nur über SMS bei Buchung, siehe `src/lib/notifications/appointment-sms.ts` und `src/app/api/notifications/route.ts`), Branchenvorlagen-Tabelle.

RLS ist vorbildlich umgesetzt (`0002_rls.sql`): `is_platform_admin()` / `is_salon_member()` als `SECURITY DEFINER`-Funktionen, jede Tabelle hat RLS aktiviert, sensible Felder (z. B. `voice_settings.phone_number`, `provider_agent_id`) sind nur per Admin-`UPDATE`-Policy änderbar; Self-Service-Felder laufen bewusst über eng gefasste `SECURITY DEFINER`-RPCs (`toggle_salon_ai`, `update_voice_settings_customer_fields`, `regenerate_calendar_feed_token`) statt über breite `UPDATE`-Policies. Dieses Muster ist die Vorlage für künftige Self-Service-Erweiterungen (siehe Phasenplan).

### 1.6 Authentifizierung und Benutzerverwaltung

Supabase Auth (E-Mail/Passwort). Zwei Sessions-Helper in `src/lib/auth/session.ts`: `requirePlatformAdmin()` und `requireSalonSession()`. Rollen sind aktuell binär (`owner`/`staff` in `salon_users.role`), es gibt **keine** granularen Rollen (Administrator/Empfang/Nur-Kalenderzugriff) und **keine** „nur eigene Termine sehen"-Einschränkung für Mitarbeiter — jeder Salon-User mit Zugriff sieht alle Daten des Salons.

### 1.7 Bestehende Mandantenfähigkeit

Vollständig und sauber über `salon_id` + RLS umgesetzt — Daten verschiedener Salons sind bereits strikt getrennt (siehe `supabase/tests/rls_and_booking.sql`, das genau das prüft). Das Konzept einer mandantenfähigen Architektur ist also **bereits vorhanden**; es muss nicht neu gebaut, sondern auf „ein Unternehmen kann mehrere Standorte/Ressourcen haben" erweitert werden.

### 1.8 Fest eingebaute Friseur-Begriffe und Friseur-Logik

**Wichtig vorab:** Die zentralen Datenfelder für Leistungen/Termine sind bereits branchenneutral benannt (`services`, `appointments`, `employees` — keine Spalten wie `haircut_type` o. Ä.). Die „Friseur-Logik" steckt weniger in starren Datenfeldern als in **drei Kategorien**:

**a) Wording auf Landingpage und in rechtlichen Seiten (reiner Text, geringes Risiko):**
- „Friseursalons" explizit in `src/app/layout.tsx:21` (Meta-Description)
- „Salon-Kalender" in `src/app/page.tsx:35`, `src/components/marketing/demo-animation.tsx:14`, `src/components/marketing/product-showcase.tsx:9`
- „Warum Salons HalloMia lieben", „Salon-Inhaber:innen", „entspannte Salons" in `src/app/page.tsx:254,255,304,346`
- sr-only-H1 „…KI-Telefonassistenz für Friseursalons…" in `src/app/page.tsx:149`
- „Salon-Mitarbeitenden", „Salons (Anruferinnen/Anrufer)" mehrfach in `src/app/datenschutz/page.tsx`
- Preistabelle nennt einen Plan explizit „Salon" (`src/components/marketing/pricing.tsx:19,25,30`)

**b) UI-Text im Admin-/App-Portal, der „Salon" statt „Unternehmen" zeigt (mittleres Risiko, sichtbar für Endkunden):**
- Platzhalter-Anzeigename `avatarLabel={session.email ?? "Salon"}` an sieben Stellen im Salon-Portal (`src/app/app/(portal)/{calendar,dashboard,appointments,absences,customers/[customerId],customers,calls,ai}/page.tsx`)
- Fehlermeldungen „kein Salon hinterlegt" (`src/app/app/login/page.tsx:7`, `src/app/admin/login/page.tsx:8`)
- Admin-Oberfläche komplett „Salon"-zentriert: Navigation „Salons" (`src/app/admin/(portal)/layout.tsx:11`), Seiten „Salons"/„Neuer Salon" (`src/components/admin/new-salon-button.tsx:32,88`, `src/app/admin/(portal)/salons/page.tsx`), „Salon-Status" (`src/components/admin/salon-settings-form.tsx:67`)
- Insgesamt **794 Vorkommen des Substrings „salon"** über 109 Dateien (Variablen-, Typ-, Funktionsnamen wie `SalonMembership`, `ActiveSalon`, `useActiveSalon`, `resolveActiveSalonId`, `salon-context.tsx`, `salon-tabs.tsx`, `SalonProvider`) — der überwiegende Teil davon sind interne Bezeichner, nicht Nutzertext.

**c) Architektonische Annahmen, die über reines Wording hinausgehen (hohes Risiko, betrifft Datenmodell/Rechte):**
- **Ein Salon = eine Ressourcen-Art (Mitarbeiter), ein Standort.** Es gibt keine Tabelle für Räume/Fahrzeuge/Geräte/Tische und keine Standort-Entität — ein Betrieb mit mehreren Filialen oder mit Ressourcen wie „Werkstattbucht" oder „Behandlungsraum" lässt sich heute nicht abbilden.
- **„Managed Service"-Rechtemodell:** RLS erlaubt Salon-Usern nur `SELECT` auf `employees`, `services`, `employee_working_hours`, `business_hours`, `voice_settings` — **Schreiben ist Plattform-Admin-only** (`0002_rls.sql:76-121,171-187`). Das ist eine bewusste Architekturentscheidung des bisherigen Produkts (HalloMia-Team richtet jeden Salon manuell ein), steht aber im direkten Widerspruch zum neuen Konzept, das ein **Self-Service-Onboarding** durch den Unternehmer selbst verlangt (Terminarten, Mitarbeiter, Öffnungszeiten, KI-Konfiguration eigenständig einrichten).
- **Fester Status-/Quellen-Wertebereich** in `appointments.status` (`booked/completed/cancelled/no_show`) und `appointments.source` (`voice_ai/manual/online_booking`) deckt die im Konzept geforderten Zustände (Angefragt, Eingecheckt, In Bearbeitung, Rückruf erforderlich; Quellen Import/Externer Kalender) nicht ab.
- **Rollenmodell** nur `owner`/`staff`, keine feingranularen Rollen und kein „nur eigene Termine"-Scoping.
- Sprachlich ist die KI selbst laut `src/lib/voice/prompt.ts:15` explizit „eine echte Mitarbeiterin am Empfang von [Salonname]" — branchenneutral formulierbar, aber aktuell fix als Empfangs-Rolle beschrieben (passt nicht 1:1 z. B. für Handwerksbetriebe ohne Empfang).
- Demo-/Seed-Daten (`supabase/seed.sql`) bilden ausschließlich einen Friseursalon ab (Damen-/Herrenschnitt, Farbe, Balayage, Bart) — es gibt keine Multi-Branchen-Demodaten.

### 1.9 Aktueller Kalender und Terminabläufe

Bereits im Detail unter 1.4 beschrieben. Buchungsablauf: `src/lib/scheduling/availability.ts` (reine Slot-Berechnung: Öffnungszeiten ∩ Arbeitszeiten − Abwesenheiten − bestehende Termine, Puffer, Vorlaufzeit, max. Vorausbuchung, unit-getestet) → `src/lib/scheduling/engine.ts` (`checkAvailability`, Buchen/Verschieben/Stornieren, jede Buchung wird unmittelbar vor dem Schreiben erneut validiert) → `book_appointment`-RPC + `appointments_no_overlap`-Exclusion-Constraint als harte Datenbankgarantie gegen Doppelbuchung, auch bei gleichzeitigen Anrufen. Diese Kernlogik ist branchenneutral und sollte **nicht** angetastet werden, nur um generische Ressourcen erweitert.

### 1.10 Demo-Daten

Nur ein einziges, vollständig ausgeprägtes Demo-Unternehmen: „Hair Lounge Milano" (3 Mitarbeiter, 6 Leistungen, Öffnungszeiten, Kunden, heutige Termine, Anrufe, Rückrufe) über `supabase/seed.sql`. Kein zweites Demo-Unternehmen, keine andere Branche. `npm run seed` führt zusätzlich `scripts/seed.ts` aus (TypeScript-Variante, nicht im Detail geprüft, aber laut `package.json` vorhanden).

### 1.11 Mobile Darstellung

Bereits deutlich weiter als der Konzeptbrief vermuten lässt: Bottom-Nav (`mobile-nav.tsx`), eigene Mobile-Agenda-Ansicht mit horizontaler Datumsleiste (`mobile-date-strip.tsx`, `mobile-agenda.tsx`) statt verkleinerter Desktop-Woche — **die zentrale Konzeptanforderung „Tagesansicht + horizontale Datumsauswahl auf dem Smartphone" ist bereits erfüllt**. Floating-Action-Button für „Neuer Termin" auf Mobile (`calendar-shell.tsx:381-389`). Nicht geprüft wurden alle Admin-Unterseiten (Tabellen wie `service-employee-matrix.tsx` sind auf kleinen Screens potenziell breiter als der Viewport) — das sollte pro Phase mit echtem Breakpoint-Test verifiziert werden, da hierzu keine automatisierten Tests existieren.

### 1.12 Vorhandene funktionslose Schaltflächen

Keine gefunden. Gezielte Suche nach `TODO`, `FIXME`, „coming soon", „not implemented" sowie leeren `onClick={() => {}}`-Handlern in `src/` ergab **keine Treffer**. Alle Buttons sind funktional an Server Actions oder State angebunden.

### 1.13 TypeScript-, Build- und Lint-Status (diagnostisch geprüft, keine Änderungen vorgenommen)

| Prüfung | Befehl | Ergebnis |
|---|---|---|
| TypeScript | `npx tsc --noEmit` | ✅ **0 Fehler** |
| Lint | `npx eslint .` | ✅ **0 Fehler**, 1 Warnung (überflüssige `eslint-disable`-Direktive in `src/components/calendar/new-appointment-modal.tsx:60`) |
| Build | `npm run build` | ✅ **Erfolgreich**, alle 28 Routen kompilieren, Typprüfung im Build ebenfalls grün |

**Fazit:** Das Projekt befindet sich in einem sauberen, deploybaren Zustand. Jede Phase des folgenden Plans muss diesen Zustand am Ende erneut erreichen (kein Merge mit roten Checks).

---

## 2. Risiken für bestehende Daten und Funktionen

1. **Exclusion-Constraint an `status = 'booked'` gekoppelt** (`0001_schema.sql:150-153`). Eine Erweiterung des Status-Wertebereichs (z. B. „Angefragt", „Eingecheckt") darf die Doppelbuchungs-Garantie nicht aufweichen — jeder neue Status muss explizit einsortiert werden (blockiert er einen Slot oder nicht?), sonst drohen stille Doppelbuchungen.
2. **RLS-Vertrauensgrenze beim Übergang vom „Managed Service"- zum Self-Service-Modell.** Aktuell dürfen Salon-User `employees`/`services`/`business_hours`/`voice_settings` nur lesen. Self-Service-Onboarding erfordert neue Schreibrechte — falls diese als breite `UPDATE`-Policies statt als eng gefasste `SECURITY DEFINER`-RPCs (Vorbild: `toggle_salon_ai`, `update_voice_settings_customer_fields`) umgesetzt werden, könnten Salon-User versehentlich technische/abrechnungsrelevante Felder ändern (z. B. `voice_settings.phone_number`, `provider_agent_id`, `salons.status`).
3. **Realtime-Publikation & Live-Kalender** (`0006_realtime_appointments.sql`, Channel-Filter `salon_id=eq.${salonId}` in `calendar-shell.tsx:155`). Jede Umbenennung von `salon_id` oder Änderung an `appointments` muss die Publikation und den Client-Filter synchron halten, sonst bleibt der Live-Kalender stumm, ohne dass ein Fehler sichtbar wird.
4. **Sprachagent-Prompt & Tool-Schema** (`src/lib/voice/prompt.ts`, `src/lib/voice/tools.ts`, `boosted-keywords.ts`) sind auf „ein Mitarbeiter, eine Leistung, ein Empfang" zugeschnitten. Neue Konzepte (Ressourcen, Standorte, individuelle Buchungsfragen) müssen in Prompt **und** Tool-JSON-Schemas **und** die Retell-/ElevenLabs-Provider-Adapter gleichzeitig einfließen — ein Auseinanderlaufen führt zu einer KI, die Dinge anbietet, die die Buchungs-Engine nicht mehr abbilden kann (Verstoß gegen die Kernregel „keine Informationen erfinden").
5. **Keine automatisierten Integrationstests außer `tests/availability.test.ts` und dem manuellen SQL-Skript `supabase/tests/rls_and_booking.sql`.** Jede Schema-Änderung an mandantenscoped Tabellen sollte dieses SQL-Skript erneut durchlaufen (Tenant-Isolation + Doppelbuchungs-Constraint sind sonst nicht abgesichert).
6. **Seed-Daten sind produktionsnah dokumentiert** (README verweist auf reale Zugangsdaten-Platzhalter, die vor Produktivbetrieb geändert werden müssen). Neue Multi-Branchen-Demodaten dürfen den bestehenden Seed-Ablauf nicht brechen und keine Klartext-Zugangsdaten einführen.
7. **Zwei parallele Voice-Provider (Retell/ElevenLabs) mit je eigenen Sync-/Webhook-Pfaden** (`src/lib/actions/{retell,elevenlabs}.ts`, `src/app/api/voice/webhook/**`, `src/app/api/cron/resync-voice-agents/route.ts`). Änderungen an `voice_settings` (z. B. neue Spalten für Sprache/Tonalität) müssen für **beide** Provider-Adapter konsistent nachgezogen werden, sonst driftet ein Provider aus dem Sync.
8. **Interne Bezeichner (`salon_id`, `SalonMembership`, Routen `/admin/salons/…`) sind tief im Code verwoben (794 Fundstellen).** Eine vollständige Umbenennung auf „company"/„Unternehmen" ist ein großer, fehleranfälliger Change über RLS-Funktionen, RPCs, Realtime-Publikation, Retell-Webhook-URLs und Storage-Policies hinweg. Dies wird im Phasenplan bewusst als **optionaler Spätschritt mit reduzierter Priorität** eingeordnet (siehe Phase 12) — die Akzeptanzkriterien des Konzepts verlangen sichtbar generische Begriffe im Produkt, nicht zwingend generische interne DB-/Variablennamen.
9. **`sales_leads` und der Admin-Akquise-Bereich (`/admin/leads`, `/admin/signups`) gehören zum internen HalloMia-Vertrieb**, nicht zum Kundenprodukt — dürfen bei der Universalisierung nicht versehentlich mit branchenspezifischer Logik vermischt werden.
10. **Fehlende Mobile-Breakpoint-Tests für dichte Admin-Tabellen** (z. B. `service-employee-matrix.tsx`) — jede Phase, die diese Bereiche berührt, muss manuell auf Smartphone-Breite geprüft werden, da kein visueller Regressionstest existiert.

---

## 3. Priorisierter Phasenplan

Reihenfolge nach Risiko (niedrig → hoch) und Abhängigkeit. Jede Phase ist einzeln deploybar, einzeln testbar und lässt das Projekt in einem grün buildenden Zustand zurück. Migrationsnummern sind Vorschläge (`0014` aufwärts) und additiv — keine bestehende Migration wird verändert.

### Phase 1 — Terminologie-Fundament (reine Konfigurationsebene, kein Rename)
**Status: ✅ umgesetzt (2026-08-27)**, gemeinsam mit einem Teil von Phase 4 (Grundnavigation), siehe Statusnotiz unten.

**Ziel:** Eine zentrale, austauschbare Begriffsebene schaffen, ohne bestehende DB-Spalten oder Routen umzubenennen.
**Vorgehen:** Neues `src/lib/terminology.ts` (bzw. React-Context analog zu `useActiveSalon`) mit Default-Labels „Unternehmen, Kunde, Terminart, Leistung, Mitarbeiter, Ressource, Standort, Anfrage". UI-Komponenten, die aktuell hart „Salon" anzeigen (siehe 1.8b), auf dieses Label-System umstellen.
**Betroffene Dateien:** neue Datei `src/lib/terminology.ts`; alle Stellen aus 1.8b (`avatarLabel={session.email ?? "Salon"}` × 7, Fehlermeldungen in `app/login` und `admin/login`).
**DB:** keine Änderung.
**Prüf-/Akzeptanzkriterien:** `npm run build`, `npx tsc --noEmit`, `npx eslint .` grün; manuelle Sichtprüfung aller betroffenen Seiten (Desktop + Mobile), kein „Salon" mehr als Platzhalter-Text im Salon-Portal sichtbar; bestehende Funktionalität (Login, Fehlermeldungen) unverändert.

**Umsetzungsnotiz (2026-08-27):** Auf expliziten Auftrag wurde diese Phase um die im Konzept beschriebene Grundnavigation erweitert (siehe „Navigation der Anwendung" im Universal-Konzept), da beide Themen zusammen die kleinste sinnvoll abnahmefähige Einheit „Generische Begriffe und Grundnavigation" bilden. Umgesetzt:

- `src/lib/terminology.ts` neu angelegt (zentrale Default-Labels: Unternehmen, Kunde, Terminart, Leistung, Mitarbeiter, Ressource, Standort, Anfrage) und in Navigation, Topbars und Avatar-Fallbacks tatsächlich verwendet (nicht nur deklariert).
- Alle sieben `avatarLabel={session.email ?? "Salon"}`-Stellen sowie die Fehlermeldungen „kein Salon hinterlegt" in `app/login` und `admin/login` auf generische Begriffe umgestellt; „Sicherer Zugang für moderne Friseursalons" im Salon-Login sowie „Salon-Kalender" im Admin-Mitarbeiterformular generalisiert.
- Hauptnavigation des Salon-Portals (`src/app/app/(portal)/layout.tsx`) exakt auf die Konzept-Vorgabe umgestellt: Übersicht, Kalender, Anrufe, Kunden, Anfragen, Meine Mia, Leistungen, Team und Ressourcen, Statistiken, Einstellungen — Desktop-Sidebar und mobile Bottom-Nav teilen sich weiterhin dieselbe `navItems`-Liste (keine Duplikate).
- Fünf neue, an bestehende RLS-Rechte gebundene Seiten ergänzt, damit jeder neue Navigationspunkt zu einer echten, funktionierenden Seite führt statt zu einem funktionslosen Button: `/app/requests` (Anfragen — bestehende `callback_requests` generisch gerahmt, inkl. Status-Aktion), `/app/services` (Leistungen, lesend — Schreibrechte sind laut RLS bis Phase 4 Plattform-Admin-only), `/app/team` (Team und Ressourcen — Mitarbeiterliste lesend + vollständig funktionale Abwesenheitsverwaltung, ersetzt `/app/absences`), `/app/stats` (Statistiken — echte 7-Tage-Kennzahlen aus vorhandenen Tabellen, keine erfundenen Daten) und `/app/settings` (Einstellungen — Unternehmensdaten lesend, funktionierender KI-Aktiv-Schalter, Links zu den übrigen Bereichen).
- `/app/absences` bleibt als Redirect auf `/app/team` erhalten (keine toten Links für bestehende Lesezeichen); `/app/appointments` bleibt unverändert erreichbar über den bestehenden „Listenansicht"-Link im Kalender, ist aber bewusst nicht mehr in der Hauptnavigation, da das Konzept dort keine eigene „Termine"-Position vorsieht.
- Bewusst **nicht** angetastet: Landingpage- und Rechtstexte-Wording (bleibt Phase 2), interne Bezeichner wie `SalonMembership`/`salon_id` (bleibt Phase 16), Schreibrechte für Leistungen/Mitarbeiter (bleibt Phase 4) und jegliche neue Datenbankarchitektur.
- Geprüft: `npx tsc --noEmit`, `npx eslint .` und `npm run build` fehlerfrei (alle 38 Routen kompilieren), `npx vitest run` weiterhin grün. Die Login-Seite wurde per Playwright auf Desktop- und Mobile-Breite visuell verifiziert; ein vollständiger Klick-Durchlauf der neu angemeldeten Seiten war in dieser Sitzung nicht möglich, da keine gültigen Demo-Zugangsdaten für das verbundene Supabase-Projekt vorlagen (die Platzhalter aus `supabase/seed.sql` griffen nicht) — dies sollte vor dem nächsten Schritt mit echten Zugangsdaten nachgeholt werden.

### Sonderauftrag — Datenmodell, Mandantenfähigkeit und Berechtigungen
**Status: ✅ umgesetzt (2026-08-28).** Auf expliziten separaten Auftrag umgesetzt, außerhalb der obigen Phasenreihenfolge, und bewusst beschränkt auf Datenmodell + direkt zugehörige Grundlagen — **kein** Kalender-, Landingpage- oder Telefonie-Code wurde in diesem Schritt angefasst. Diese Einheit deckt inhaltlich Teile der späteren Phasen 3 (Branchenvorlagen), 6 (nur `location_id`, **nicht** die Status-/Quellen-Erweiterung — siehe Begründung unten), 7 (nur Speicherung, keine Prompt-Anbindung), 8, 9, 10, 11 (nur Datenmodell, keine Verfügbarkeitsprüfung), 12 (nur Spalten, keine Prompt-/UI-Anbindung), 13 (Rollen-Grundlage, keine Kalender-Scoping-RLS) und 17 (Benachrichtigungstabelle) ab, die dadurch in der obigen Reihenfolge entsprechend vorgezogen sind.

**Neue Migrationen** (`supabase/migrations/0014`–`0028`, additiv, keine bestehende Migration verändert, angewendet auf das verbundene Supabase-Projekt `saloncall-ai`):

- `locations` (Standorte) + `business_hour_exceptions` (Feiertage/Ausnahmen je Standort) — jeder bestehende Salon erhält automatisch einen impliziten Standard-Standort (Backfill), ein Trigger (`0027`) legt ihn für neu angelegte Salons ebenfalls automatisch an. `location_id` additiv auf `employees`, `services`, `appointments`, `business_hours`.
- `resources`, `resource_working_hours`, `service_resources`, `appointment_resources` — generische Ressourcen (Räume, Fahrzeuge, Geräte, Tische …), rein als Datenmodell; die Verfügbarkeitsprüfung bezieht sie noch nicht ein (Kalender-Phase).
- `salon_users.role` erweitert auf `owner | administrator | staff | reception | calendar_only`; neue Tabelle `role_permissions` (pro Rolle, optional pro Salon überschreibbar) plus `has_permission()`/`current_salon_role()` SQL-Funktionen — macht Berechtigungen wie gefordert konfigurierbar. Bestehende RLS-Policies auf den *vorhandenen* Tabellen (employees, services, business_hours, customers, appointments, voice_settings) wurden bewusst **nicht** umgeschrieben, um das aktuell funktionierende Managed-Service-Modell und den Kalender nicht anzufassen; neue Tabellen aus diesem Schritt nutzen `has_permission()` bereits aktiv.
- `customers`: `status`, `tags`, `custom_fields`, `address`, `company`, `preferred_location_id`, `consent_recording`, `consent_marketing`, `deleted_at` (Soft-Delete für DSGVO-Löschung).
- `services`: `custom_questions`, `required_customer_fields`, `bookable_phone`, `bookable_online`; neue Tabelle `appointment_answers` für die Antworten.
- `requests` (branchenneutrale Anfragen: Kategorie, Dringlichkeit, Rückrufzeitraum, zuständiger Mitarbeiter, Anhänge, Status) — `callback_requests` bleibt unverändert bestehen (weiterhin Basis von `/app/requests`); bestehende Zeilen wurden verlustfrei als `category = 'callback'` in `requests` übernommen, nichts wurde gelöscht oder umbenannt.
- `faq`, `industry_templates` (mit neun vorbefüllten Branchenvorlagen inkl. „Andere Branche") + `salons.industry_template_id`, `custom_field_definitions` (generisches Framework für individuelle Felder je Unternehmen).
- `voice_settings`: `assistant_name`, `formality`, `languages`, `never_mention`, `after_hours_behavior`, `handoff_number`, `urgent_keywords`, `notify_after_call` — nur die Spalten; Prompt-Aufbau, Tool-Schemas und das bestehende „Meine Mia"-Formular bleiben unverändert.
- `calls`: `summary`, `urgency`, `sentiment`, `resolved`, `consent_recording`, `recording_url`.
- `notifications` + `notification_preferences` (konfigurierbare Kanäle pro Nutzer und Ereignistyp); das tatsächliche Versenden über E-Mail/SMS/Push ist spätere Integrationsarbeit.
- DSGVO: `export_customer_data()` (vollständiger JSON-Export) und `delete_customer_data()` (Anonymisierung statt Hard-Delete, da `appointments`/`calls` per `on delete restrict`/`set null` auf `customers` verweisen) als `SECURITY DEFINER`-RPCs nach dem bestehenden Muster, beide mit Audit-Log-Eintrag.

**Bewusst nicht in diesem Schritt geändert** (jeweils mit Begründung):
- `appointments.status`/`.source` bleiben beim bisherigen Wertebereich (`booked/completed/cancelled/no_show`, `voice_ai/manual/online_booking`). Eine Erweiterung würde zwingend die Exclusion-Constraint, die Verfügbarkeitsprüfung, den Sprachagent-Prompt/die Tool-Schemas und mehrere Kalender-Komponenten (`appointment-card.tsx`, `day-view.tsx`, `week-grid.tsx`, `appointments-list-view.tsx`) gleichzeitig ändern — das ist explizit Kalender-Arbeit und wurde für diesen Schritt ausgeschlossen.
- Keine RLS-Verschärfung auf bestehenden Tabellen (z. B. „Mitarbeiter sieht nur eigene Termine") — das ist Kalender-/Termindaten-Scoping und bleibt der späteren Rollen-Phase vorbehalten; die Grundlage (`role_permissions`, `has_permission()`) steht dafür bereits bereit.
- Keine neue UI: Es gibt aktuell keine bestehende Oberfläche zur Verwaltung von Standorten, Ressourcen, Rollen pro Nutzer, FAQ, Branchenvorlagen oder Benachrichtigungs-Präferenzen — solche Bildschirme sind daher keine „direkt dazugehörigen vorhandenen Oberflächen" und wurden nicht neu gebaut. Einzige Anwendungscode-Änderung: `SalonMembership.role` in `src/lib/auth/session.ts` auf die fünf neuen Rollenwerte erweitert (einzige Stelle im Code, die den bisherigen `"owner" | "staff"`-Typ referenzierte), plus neues `src/lib/auth/permissions.ts` (Permission-Konstanten + `checkPermission()`-Helfer für künftige Server Actions).

**Geprüft:** `npx tsc --noEmit`, `npx eslint .` (0 Fehler, dieselbe vorbestehende Warnung wie in Phase 1) und `npm run build` (alle 38 Routen) fehlerfrei; `npx vitest run` weiterhin grün (10/10). `supabase/tests/rls_and_booking.sql` um Mandantentrennungs-Checks für `locations` und `requests` ergänzt und gegen das Live-Projekt ausgeführt — alle Isolations- und Anonym-Zugriffs-Checks bestehen. Der `double_booking_guard`-Check dieses Skripts schlägt aktuell fehl, aber nachweislich **nicht** durch diese Änderung: Die Demo-Seed-Daten wurden relativ zum Erstellungsdatum des Projekts (26.08.) befüllt, während der Test „heute" dynamisch berechnet; am 28.08. existiert Annas ursprünglich für „heute" gedachter 09:00–10:30-Termin nicht mehr unter dem aktuellen Datum, wodurch der Überlappungstest ins Leere läuft — ein vorbestehendes, datumsabhängiges Seed-Problem, unabhängig vom hier umgesetzten Datenmodell. `get_advisors` (Supabase) zeigt keine neuen kritischen Befunde; die wenigen WARN-Einträge zu `auth_rls_initplan` in den neuen Policies wurden in `0028_rls_perf_fix.sql` behoben, verbleibende INFO-Hinweise zu fehlenden Indizes auf einzelnen Fremdschlüsseln entsprechen dem bereits bestehenden Muster im Projekt.

### Sonderauftrag — Branchenvorlagen und Onboarding-Grundlage
**Status: ✅ umgesetzt (2026-08-28).** Auf expliziten separaten Auftrag umgesetzt, außerhalb der obigen Phasenreihenfolge. Deckt inhaltlich Phase 3 (Branchenvorlagen) vollständig ab sowie den Teil von Phase 5 (Onboarding-Assistent), der ausdrücklich beauftragt war: **nur** die Schritte 1 „Unternehmen anlegen", 2 „Branche auswählen" und 3 „Unternehmensdaten eintragen" des im Konzept beschriebenen 12-Schritte-Assistenten. Die übrigen neun Schritte (Standort, Öffnungszeiten, Team/Ressourcen, Terminarten, Meine Mia, häufige Fragen, Kalender testen, Telefonnummer verbinden, Mia aktivieren) folgen jeweils erst mit ihrer zugehörigen Fachbereichs-Phase (4, 7–13) und sind hier bewusst **nicht** umgesetzt — sie erscheinen im Assistenten nur als inaktive Fortschritts-Vorschau ohne Funktion, keine funktionslosen Buttons.

**Branchenvorlagen (`industry_templates`, Migration `0029`):** Auf die im Konzept geforderten elf Branchen vervollständigt — die neun bestehenden (Friseur, Kosmetik, Physiotherapie, Arztpraxis, Handwerk, Beratung, Werkstatt, Restaurant, Andere Branche) plus zwei neue: **Kanzlei** (`legal`) und **Fotografie** (`photography`). Jede Vorlage (außer „Andere Branche", bewusst leer) liefert jetzt zusätzlich zu den bereits vorhandenen Beispiel-Terminarten auch Vorschläge für `example_custom_questions` (individuelle Buchungsfragen) und die neue Spalte `example_required_fields` (notwendige Kundenangaben) — beides zuvor für alle Vorlagen leer.

**Self-Service-Grundlage für Onboarding (Migrationen `0029`–`0030`):** Bislang durften Unternehmen (`salons`) laut RLS ausschließlich von Plattform-Admins angelegt/geändert werden (Managed-Service-Modell, siehe Risiko 2 oben). Für die Schritte 1–3 des Assistenten waren daher zwei eng gefasste `SECURITY DEFINER`-RPCs nach dem etablierten Muster (Vorbild `toggle_salon_ai`, `update_voice_settings_customer_fields`) nötig, **ohne** die bestehende `salons`-RLS-Policy selbst zu lockern:
- `create_own_salon(name, slug)` — legt für den aktuell angemeldeten Nutzer ein eigenes Unternehmen an (Status `trial`, KI zunächst inaktiv), macht ihn zum `owner` in `salon_users` und seedet Standard-Öffnungszeiten/`voice_settings` wie beim admin-seitigen Anlegen. Lehnt einen zweiten Aufruf für denselben Nutzer ab („already has a company").
- `update_own_salon_onboarding(...)` — pflegt Name/Kurzname, Branche, Kontakt/Adresse/Zeitzone sowie den editierbaren Vorschlags-Entwurf (`onboarding_draft`) und den Fortschritts-Zeiger (`onboarding_step`, monotonic über `greatest()`); geprüft über `is_salon_member()`, alle Parameter einzeln optional für granulare Autospeicherung.

Neue Spalten auf `salons`: `onboarding_step` (höchster erreichter Schritt, 1–12), `onboarding_draft` (jsonb, die editierbaren/löschbaren Branchen-Vorschläge) und `onboarding_completed_at` (bleibt `null`, bis irgendwann der komplette 12-Schritte-Assistent abgeschlossen ist — für alle bereits bestehenden, admin-seitig angelegten Salons rückwirkend auf „abgeschlossen" gesetzt, damit für sie kein Einrichtungs-Hinweis erscheint).

**Einrichtungsassistent (`/app/onboarding`):** Neue, eigenständige Route außerhalb des bestehenden Portal-Layouts (kein Sidebar/Salon-Kontext nötig, solange noch kein Unternehmen existiert). `requireSalonSession()` leitet einen angemeldeten Nutzer ohne Unternehmen jetzt hierher um statt auf eine Fehlerseite. Der Assistent zeigt eine Fortschrittsanzeige über alle zwölf Konzept-Schritte (die ersten drei klickbar/aktiv, die übrigen sichtbar aber inaktiv „folgt später"), speichert jedes Feld automatisch nach kurzer Inaktivität (debounced Server Actions, kein „Speichern"-Klick nötig) und lässt jederzeit Zurück-Navigation zwischen den drei Schritten zu, ohne Daten zu verlieren.

- **Schritt 1 „Unternehmen":** Name + Kurzname (Slug), danach jederzeit erneut bearbeitbar.
- **Schritt 2 „Branche":** Elf Branchen-Karten; Auswahl kopiert die Vorlagen-Vorschläge (Terminarten, Buchungsfragen, benötigte Angaben) in drei editierbare Listen, in denen jeder Eintrag umbenannt, gelöscht oder frei ergänzt werden kann. Ein Branchenwechsel ersetzt nur diese Entwurfs-Vorschläge — nichts davon ist bereits eine echte Terminart, kein bestehender Code liest `industry_template_id` für Verzweigungen, die Auswahl ist beliebig oft reversibel. „Andere Branche" liefert bewusst keine Vorschläge und funktioniert rein manuell.
- **Schritt 3 „Unternehmensdaten":** Telefonnummer, Adresse, Zeitzone; Abschluss führt zurück ins Dashboard, das dort einen „Einrichtung unvollständig"-Hinweis mit Link zurück zum Assistenten zeigt, solange `onboarding_completed_at` `null` ist.

**Bewusst nicht in diesem Schritt umgesetzt:** kein neues Self-Service-Signup/Auth-Flow — Zugangsdaten werden weiterhin wie bisher vom HalloMia-Team vergeben, der Assistent greift ab dem ersten Login eines Nutzers ohne Unternehmen; keine Erstellung echter `services`-Zeilen aus den Vorlagen-Vorschlägen (das ist Schritt 7 „Terminarten und Leistungen anlegen", eigene Fachbereichs-Phase, dort werden die hier gesammelten Entwurfsdaten weiterverwendet); keine Änderung an der bestehenden `salons`-RLS-Policy selbst (bleibt admin-only, nur die beiden neuen RPCs öffnen gezielt Self-Service).

**Getestet:** `npx tsc --noEmit`, `npx eslint .` (0 Fehler, dieselbe vorbestehende Warnung wie in Phase 1) und `npm run build` (alle 39 Routen, inkl. `/app/onboarding`) fehlerfrei; `npx vitest run` weiterhin grün (10/10). Der browserbasierte Klick-Durchlauf war in dieser Sitzung technisch nicht möglich (die Sandbox-Netzwerkrichtlinie blockiert ausgehende Verbindungen zum Supabase-Projekt für direkt gestartete Prozesse, auch den lokalen `next dev`-Server — dieselbe Einschränkung, die vermutlich schon den in Phase 1 dokumentierten Klick-Durchlauf verhindert hat). Stattdessen wurden beide neuen RPCs direkt gegen das Live-Projekt mit einem temporären Testnutzer verifiziert (danach vollständig entfernt): Unternehmen anlegen inkl. Seed-Daten, Branche wählen und wechseln (Entwurf wird korrekt ersetzt, `onboarding_step` bleibt monoton), Unternehmensdaten speichern, sowie beide Schutzmechanismen (fremder Nutzer kann kein fremdes Unternehmen ändern; ein Nutzer kann kein zweites Unternehmen anlegen). Das bestehende Demo-Unternehmen „Hair Lounge Milano" bleibt unverändert isoliert (`onboarding_step`/`onboarding_completed_at` nur rückwirkend befüllt, keine Vermischung mit neuen Branchen-Testdaten).

### Sonderauftrag — Standorte, Team, Ressourcen und Verfügbarkeit
**Status: ✅ umgesetzt (2026-08-28).** Auf expliziten separaten Auftrag umgesetzt, außerhalb der obigen Phasenreihenfolge und ausdrücklich beschränkt auf genau diesen Konzeptabschnitt. Baut auf dem Datenmodell des Sonderauftrags „Datenmodell, Mandantenfähigkeit und Berechtigungen" auf (`locations`, `resources`, `resource_working_hours`, `business_hour_exceptions`, `role_permissions`/`has_permission()`) und macht daraus **echte, bedienbare Self-Service-Oberflächen** — dort bewusst zurückgestellt, weil zu diesem Zeitpunkt weder Schreibrechte noch UI dafür existierten. Deckt inhaltlich Phase 4 (Self-Service-Rechte, nur für Mitarbeiter/Ressourcen/Standorte/Öffnungszeiten — **nicht** für Terminarten/Leistungen, das bleibt separat), Phase 10 (Standorte) und Phase 11 (Ressourcen, beschränkt auf Datenmodell + Verfügbarkeitsprüfung; Kalender-UI-Ressourcenspalten bleiben Phase 14) sowie den Rest von Phase 5 (Onboarding-Schritte 4–6) ab.

**Neue Migrationen** (`supabase/migrations/0031`–`0032`, additiv, angewendet auf das verbundene Supabase-Projekt `saloncall-ai`):
- Self-Service-RLS: `employees`, `employee_working_hours`, `resources`, `resource_working_hours`, `service_resources`, `locations`, `business_hours`, `business_hour_exceptions` wechseln von „nur Plattform-Admin schreibt" auf `has_permission(salon_id, 'manage_team' | 'manage_settings')` — exakt das in `0016_roles_permissions.sql` vorbereitete Berechtigungsmodell, erstmals produktiv für Schreibzugriffe genutzt. `employee_absences` war bereits self-service und bleibt unverändert.
- Neue Tabelle `callback_windows` (Konzept: „Zeiträume für telefonische Rückrufe") — mehrere Zeitfenster pro Wochentag, orthogonal zu den regulären Öffnungszeiten.
- `salons.max_parallel_appointments` (nullable, „unbegrenzt" per Default — siehe `0032`, Korrektur eines zu strengen Defaults aus `0031`) und `salons.max_appointments_per_day` (nullable) ergänzen die bereits vorhandenen `slot_granularity_minutes`/`earliest_booking_lead_minutes`/`max_advance_booking_days`. Eine enge `update_own_booking_rules()`-RPC (statt einer breiten `UPDATE`-Policy auf `salons`, das auch geschützte Felder wie `status`/`slug`/`ai_active` trägt) schreibt diese fünf Felder.
- **Korrektur während der Umsetzung:** `0031` setzte `max_parallel_appointments` zunächst auf „Standard 1" — das hätte jeden bestehenden Mehr-Mitarbeiter-Salon (z. B. „Hair Lounge Milano") rückwirkend auf einen einzigen gleichzeitigen Termin unternehmensweit begrenzt, sobald die Verfügbarkeitsprüfung das Feld liest. `0032` behebt das vor jeglicher Auswirkung auf echte Daten (`alter column ... drop not null/default`, bestehende Zeilen auf `null` zurückgesetzt) — ein Beleg dafür, warum jede neue Buchungsregel gegen bestehende Demo-/Kundendaten gedacht werden muss, nicht nur gegen den Konzepttext.

**Verfügbarkeitsprüfung erweitert** (`src/lib/scheduling/availability.ts`, `engine.ts`) — additiv, alle neuen Parameter optional und ohne Wirkung, wenn nicht gesetzt (bestehende Aufrufer und alle zehn ursprünglichen Testfälle unverändert grün):
- Ein inaktiver Mitarbeiter oder eine gesperrte/inaktive Ressource liefert jetzt auch auf Engine-Ebene nie mehr einen Slot (zusätzlich zur bereits bestehenden Filterung `active = true` in den Buchungs-Dropdowns — echte Verteidigung in der Tiefe, nicht nur UI-Kosmetik).
- Eine optional angegebene Ressource wird mit ihren eigenen Arbeitszeiten geschnitten und ihre bestehenden Buchungen (`appointment_resources`) wie die des Mitarbeiters als belegt behandelt.
- `max_appointments_per_day` (pro Mitarbeiter und Tag) und `max_parallel_appointments` (unternehmensweit, unabhängig vom bestehenden harten Pro-Mitarbeiter-Doppelbuchungsschutz) werden ausgewertet.
- `tests/availability.test.ts` um neun neue Fälle ergänzt (inaktiver Mitarbeiter, gesperrte/gebuchte/ohne-Arbeitszeiten-Ressource, Tages- und Parallel-Limit über/unter Schwelle, Rückwärtskompatibilität ohne die neuen Felder) — 19/19 grün (vorher 10/10).

**Neue Self-Service-Oberflächen** (Desktop und Smartphone, gleiche Formularmuster wie die bestehenden Admin-Ansichten — Karten mit Bearbeiten-Modal, große Touch-Flächen, echte serverseitige Validierung via Zod plus RLS als zweite Verteidigungslinie):
- `/app/team` (bisher rein lesend) ist jetzt eine vollständige Verwaltung: Mitarbeiter anlegen/bearbeiten/löschen (inkl. Standort-Zuordnung), Arbeitszeiten pro Mitarbeiter, Ressourcen anlegen/bearbeiten/löschen (Name, Typ — Raum/Behandlungsplatz/Fahrzeug/Gerät/Tisch/Sonstige —, Beschreibung, Standort, Farbe, Aktiv-Status), Verfügbarkeitszeiten pro Ressource, sowie die bereits bestehende Abwesenheitsverwaltung (Urlaub/Krankheit/Pause).
- Neue Seite `/app/locations`: Standorte anlegen/bearbeiten/löschen, Standard-Standort festlegen (genau einer pro Unternehmen, technisch über den bestehenden partiellen Unique-Index abgesichert; der Standard-Standort selbst ist nicht löschbar, und es muss immer mindestens ein Standort bestehen bleiben).
- Neue Seite `/app/availability`: reguläre Öffnungszeiten (self-service-Variante des bestehenden Formulars), Feiertage & abweichende Öffnungszeiten je Standort, Buchungsregeln (Vorlaufzeit, Buchungszeitraum, Intervall, parallele Termine, maximale Termine pro Tag), Rückrufzeiträume.
- Beide neuen Seiten sind aus „Einstellungen" verlinkt; die Hauptnavigation selbst bleibt exakt wie im Konzept vorgegeben (keine zusätzlichen Top-Level-Punkte).
- Jede Aktion prüft serverseitig `has_permission()` erneut (nicht nur clientseitig ausgeblendet); Nutzer ohne `manage_team`/`manage_settings` sehen die Verwaltung nur lesend.

**Onboarding-Schritte 4–6 ergänzt** (`src/components/onboarding/{location,hours,team}-step.tsx`, `lib/onboarding/steps.ts`, `onboarding-wizard.tsx`): „Standort" (nutzt denselben Standort-Manager wie `/app/locations` — der automatisch angelegte Default-Standort ist bereits vorhanden und kann direkt bearbeitet werden), „Öffnungszeiten" (dieselbe Öffnungszeiten-Komponente wie `/app/availability`) und „Team & Ressourcen" (dieselben Mitarbeiter-/Ressourcen-Manager wie `/app/team`) sind jetzt interaktiv (`LAST_INTERACTIVE_STEP` 3 → 6); Schritt 3 „Unternehmensdaten" führt nicht mehr direkt ins Dashboard, sondern zu Schritt 4 weiter. Die übrigen sechs Schritte bleiben als inaktive Fortschritts-Vorschau bestehen. Komponenten werden 1:1 zwischen Onboarding und den regulären Verwaltungsseiten wiederverwendet (keine Duplikate).

**Bewusst nicht in diesem Schritt umgesetzt** (jeweils mit Begründung):
- Terminarten/Leistungen bleiben admin-only (unverändert Phase 4 in der Restform) — das ist ein eigener Konzeptabschnitt („Flexible Terminarten"), nicht Teil von „Standorte, Team, Ressourcen und Verfügbarkeit".
- Die Verfügbarkeitsprüfung bezieht Ressourcen nur ein, wenn eine `resourceId` explizit übergeben wird — es gibt noch keinen Kalender-Flow, der eine Ressource verpflichtend an eine Terminart koppelt und automatisch auswählt (das wäre der in Phase 11 als „aufwendigster fachlicher Teil" markierte Schritt: Terminart ↔ Ressourcen-Pflichtzuordnung, Ressourcenspalten im Kalender, automatische Ressourcenauswahl bei der Buchung). Die Grundlage (Engine-Parameter, RLS, `appointment_resources`) steht dafür bereit.
- `service_resources` (Terminart ↔ Ressource) erhielt dieselbe Self-Service-RLS wie die übrigen neuen Tabellen, aber noch keine UI — dafür fehlt die Terminarten-Verwaltung selbst noch (siehe oben).
- Keine Änderung an `appointments.status`/`.source` oder der Exclusion-Constraint — „parallele Termine" ist bewusst als zusätzliche, weichere unternehmensweite Obergrenze umgesetzt, nicht als Aufweichung des harten Pro-Mitarbeiter-Doppelbuchungsschutzes (Risiko 1 im Umsetzungsplan).

**Geprüft:** `npx tsc --noEmit`, `npx eslint .` (0 Fehler, dieselbe vorbestehende Warnung wie in Phase 1) und `npm run build` (alle 41 Routen, inkl. `/app/locations` und `/app/availability`) fehlerfrei; `npx vitest run` 19/19 grün. `supabase/tests/rls_and_booking.sql` um einen Self-Service-Block (2c) ergänzt und gegen das Live-Projekt ausgeführt: Anlegen/Ändern/Löschen eigener Mitarbeiter, Ressourcen und Standorte funktioniert, Schreibversuche in ein fremdes Unternehmen bleiben wirkungslos bzw. werden abgelehnt — alle elf Prüfpunkte des Skripts (inkl. `double_booking_guard` und der bestehenden Mandantentrennungs-Checks) sind PASS. `get_advisors` zeigt keine neuen kritischen Befunde; die neuen Policies folgen demselben Muster wie die bestehenden (keine zusätzlichen `auth_rls_initplan`-Warnungen), verbleibende INFO-Hinweise zu fehlenden Fremdschlüssel-Indizes entsprechen dem bereits bestehenden, dokumentierten Muster im Projekt.

### Sonderauftrag — Eigener HalloMia-Kalender (vollständig bedienbar)
**Status: ✅ umgesetzt (2026-08-28).** Auf expliziten separaten Auftrag umgesetzt, außerhalb der obigen Phasenreihenfolge und ausdrücklich **beschränkt auf den Kalender selbst** — Telefonie (`src/lib/voice/**`), Dashboard, Statistiken, Kundenprofil und Admin-Terminliste wurden bewusst nicht angefasst. Deckt Phase 14 vollständig ab (Suche, Filter nach Standort/Terminart/Status, Verlängern/Verkürzen per Drag) sowie den Rest der im Universal-Konzept unter „Eigener HalloMia-Kalender"/„Terminverwaltung" geforderten Punkte, die zuvor laut Abschnitt 1.4/1.9 noch fehlten, und den Onboarding-Schritt 10 „Kalender testen" aus Phase 5.

**Suche und Filter** (`src/lib/scheduling/calendar-filters.ts`, `src/components/calendar/multi-select-filter.tsx`, `calendar-shell.tsx`): eine zentrale, reine `matchesCalendarFilters()`-Funktion (unit-getestet, siehe unten) wertet Mitarbeiter-, Standort-, Terminart- und Status-Filter sowie eine Volltextsuche (Kundenname, Telefonnummer, Notiz, Leistungsname) konsistent in Tages-, Wochen-, Monats- und der neuen Listenansicht aus. Der bisherige `EmployeeFilter` wurde durch die generische `MultiSelectFilter`-Komponente ersetzt (vier Instanzen statt vier Implementierungen). Der Standort einer Terminbuchung wird dabei vom zugewiesenen Mitarbeiter abgeleitet (`employees.location_id`, seit dem Datenmodell-Sonderauftrag vorhanden) — ohne die Buchungs-RPC oder bestehende Termine anzufassen; der Standort-Filter blendet sich bei Ein-Standort-Unternehmen (der heutige Regelfall) automatisch aus. Terminarten-Filter nutzt die bereits im Termin enthaltenen `services`; Status-Filter zeigt standardmäßig alles außer „Storniert" (bisheriges Verhalten der Kalenderabfragen), lässt „Storniert" aber gezielt sichtbar machen — dafür liefern `getDayCalendarDataAction`/`getWeekCalendarDataAction`/die neue `getListCalendarDataAction`/`getWeekOverviewAction` jetzt alle Status statt serverseitig zu filtern.
**Dauer ändern (Resize):** `src/lib/scheduling/engine.ts` (`resizeAppointment`) verschiebt nur `end_at`, `start_at` bleibt fest; Kollisionssicherheit kommt von derselben `appointments_no_overlap`-Exclusion-Constraint wie bei Erstellung/Verschieben (Fehlercode `23P01` → „Dieser Zeitraum ist nicht verfügbar."). In Tages- und Wochenansicht gibt es dafür einen Greif-Griff am unteren Kartenrand (Pointer-Events, wie das bestehende Verschieben-per-Drag), zusätzlich ein Zahlenfeld „Dauer ändern" im Detaildialog als tastatur-/klickbare Alternative auf Geräten ohne Drag.
**Terminliste als vierte Kalenderansicht** (`calendar-list-view.tsx`): bisher gab es nur eine separate Terminliste außerhalb des Kalenders (`/app/appointments`); jetzt ist „Liste" ein gleichwertiger Tab neben Tag/Woche/Monat, gruppiert nach Tag, mit denselben Filtern/Suche und Klick-zu-Detaildialog.
**Status und Buchungsquelle:** `src/lib/scheduling/status.ts` zentralisiert deutsche Labels/Farben für die vier bestehenden Status (`booked/completed/cancelled/no_show`) und drei Quellen (`voice_ai/manual/online_booking`), verwendet in Terminkarte, Detaildialog und der neuen Listenansicht; stornierte Termine werden in der Kartenansicht jetzt sichtbar durchgestrichen/abgeblendet statt (wie zuvor) serverseitig komplett ausgeblendet zu sein.
**Onboarding-Schritt 10 „Kalender testen"** (`src/components/onboarding/calendar-test-step.tsx`, `src/lib/onboarding/steps.ts`): nutzt denselben `checkAvailabilityAction`/`createManualAppointmentAction`/`cancelAppointmentAction`-Pfad wie der reguläre Kalender, um einmal vollständig Verfügbarkeit prüfen → Testtermin buchen → wieder stornieren durchzuspielen — kein separater Testmodus, sondern derselbe Code, den auch Mia am Telefon nutzt. `LAST_INTERACTIVE_STEP` von 7 auf 10 angehoben (Schritt 7 „Terminarten & Leistungen" führt jetzt direkt zu Schritt 10 statt den Assistenten zu verlassen); Schritte 8/9 („Meine Mia", „Häufige Fragen") bleiben bewusst unangetastete Fortschritts-Vorschauen, da sie nicht zum Kalender gehören.

**Bewusst nicht in diesem Schritt umgesetzt** (jeweils mit Begründung):
- Der im Konzept unter „Terminverwaltung" vollständige Status-Wertebereich (Angefragt, Eingecheckt, In Bearbeitung, Rückruf erforderlich) und die zusätzlichen Buchungsquellen (Import, Externer Kalender) bleiben **unverändert bei den bestehenden vier/drei Werten**. Das ist weiterhin Phase 6 aus diesem Plan: eine Erweiterung würde zwingend die Exclusion-Constraint, `src/lib/voice/tools.ts` (KI-Buchung), Dashboard, Statistiken und das Kundenprofil gleichzeitig ändern — bei einem Auftrag, der ausdrücklich auf „den eigenen Kalender" beschränkt war, wäre das eine unautorisierte Ausweitung auf Telefonie- und Auswertungscode. Status und Buchungsquelle sind in diesem Schritt stattdessen als vollwertige Filter-/Anzeige-Dimensionen im Kalender selbst umgesetzt (siehe oben).
- Keine Ressourcen-Spalten/-Filter im Kalender (Terminart↔Ressourcen-Pflichtzuordnung, automatische Ressourcenauswahl) — das bleibt der in Phase 11 als „aufwendigster Teil" markierte, noch offene Rest.
- Resize ist auf `status = "booked"` beschränkt (wie das bestehende Verschieben) und validiert serverseitig eine Mindestdauer von 5 Minuten; keine Vor-/Nachbereitungszeit-Neuberechnung beim Resize, da eine manuell verlängerte/verkürzte Dauer bewusst die Servicedauer überschreiben darf (Konzept: „Dauer durch Vergrößern oder Verkürzen ändern" als eigenständige Aktion, nicht als Leistungs-Neukonfiguration).
- Monatsansicht filtert weiterhin auf Basis der schlanken `getWeekOverviewAction`-Zeilen (kein Telefonnummer-Abgleich bei der Suche dort, da diese Abfrage aus Performance-Gründen keine Telefonnummer lädt) — Tages-, Wochen- und Listenansicht durchsuchen Telefonnummern vollständig.

**Geprüft:** `npx tsc --noEmit`, `npx eslint .` (0 Fehler, dieselbe vorbestehende Warnung wie in Phase 1) und `npm run build` fehlerfrei (keine neue Route in diesem Schritt — reine Kalender-/Onboarding-Komponentenarbeit); `npx vitest run` 31/31 grün (11 neue Tests gegenüber dem vorherigen Stand von 20: sieben Fälle für `matchesCalendarFilters` in `tests/calendar-filters.test.ts` und vier neue Kern-Terminablauf-Fälle — Erstellen/Verschieben blockiert den Slot, Stornieren gibt ihn frei, Verlängern entfernt nicht mehr passende Startzeiten, Verkürzen macht zuvor kollidierende Startzeiten wieder frei — in `tests/availability.test.ts`).

### Phase 2 — Landingpage & rechtliche Seiten universalisieren
**Ziel:** Konzept-Akzeptanzkriterium „Landingpage wirkt nicht mehr wie reine Friseur-Software" erfüllen.
**Vorgehen:** Neue Hero-Headline/Unterzeile gemäß Konzept, Branchen-Karten-Sektion (sechs Branchen + „Weitere Branchen anzeigen"), Ersetzen aller Friseur-spezifischen Formulierungen aus 1.8a durch branchenneutrale bzw. gemischte Beispiele. `datenschutz`/`impressum` sprachlich anpassen („Salon" → „Unternehmen", ohne Rechtsinhalte zu verändern).
**Betroffene Dateien:** `src/app/page.tsx`, `src/app/layout.tsx` (Meta-Description), `src/components/marketing/*`, `src/app/datenschutz/page.tsx`, `src/app/impressum/page.tsx`.
**DB:** keine Änderung.
**Prüf-/Akzeptanzkriterien:** Visuelle Prüfung Desktop/Tablet/Mobile; keine erfundenen Kundennamen/Bewertungszahlen (Konzept-Vorgabe); Build/Lint/TSC grün; bestehende Anchor-Links (`#funktionen` etc.) funktionieren weiter.

### Phase 3 — Branchenvorlagen-Datenmodell (additiv)
**Status: ✅ umgesetzt** im Sonderauftrag „Branchenvorlagen und Onboarding-Grundlage" oben (Migration `0029`, elf Branchen statt der ursprünglich neun vorgeschlagenen, inkl. Buchungsfragen/benötigte Felder je Vorlage).
**Ziel:** Grundlage für „Branche auswählen → passende Vorschläge" schaffen, ohne bestehende Logik zu verändern.
**Vorgehen:** Neue Migration `0014_industry_templates.sql`: Tabelle `industry_templates` (Schlüssel, Anzeigename, Vorschlags-Terminologie als jsonb, Beispiel-Terminarten als jsonb) + nullable Spalte `salons.industry_template_id`. Nur lesend genutzt, keine bestehende Logik ändert sich, solange die Spalte `null` bleibt.
**Betroffene Dateien/DB:** neue Migration; `src/lib/supabase/database.types.ts` (regenerieren); keine Verbraucherlogik in dieser Phase.
**Prüf-/Akzeptanzkriterien:** Migration lässt sich auf einer Kopie der DB anwenden, ohne bestehende Daten zu verändern; `supabase/tests/rls_and_booking.sql` läuft weiterhin fehlerfrei; Build/TSC grün.

### Phase 4 — Self-Service-Rechte für Terminarten/Mitarbeiter/Öffnungszeiten (RLS-Erweiterung)
**Status: ✅ umgesetzt.** `employees`, `employee_working_hours`, `resources`, `resource_working_hours`, `service_resources`, `locations`, `business_hours`, `business_hour_exceptions` im Sonderauftrag „Standorte, Team, Ressourcen und Verfügbarkeit" oben; `services` und `employee_services` seither ebenfalls im Sonderauftrag „Terminarten und Leistungen (vollständig)" — `/app/services` ist jetzt vollständig self-service (Erstellen/Bearbeiten/Aktivieren/Deaktivieren/Löschen), nicht mehr nur lesend.
**Ziel:** Vom „Managed Service"- zum Self-Service-Modell wechseln (Konzept-Kernanforderung „Unternehmen verwalten alle Einstellungen selbst").
**Vorgehen:** Nach dem bestehenden Muster (`toggle_salon_ai`, `update_voice_settings_customer_fields`) neue `SECURITY DEFINER`-RPCs bzw. gezielt erweiterte `INSERT/UPDATE`-Policies für `services`, `employees`, `employee_working_hours`, `business_hours`, beschränkt auf die Rolle `owner`. Technische/abrechnungsrelevante Felder (`salons.status`, `voice_settings.phone_number/provider_*`) bleiben admin-only. Neue Formulare im Salon-Portal (`/app/services`, `/app/team`) analog zu den bestehenden Admin-Formularen (`src/components/admin/service-form.tsx`, `employee-form.tsx` als Vorlage, keine Duplikation der Komponenten, sondern Wiederverwendung mit Rollen-Prop).
**Betroffene Dateien/DB:** neue Migration `0015_self_service_rls.sql`; neue Seiten `src/app/app/(portal)/services/page.tsx`, `.../team/page.tsx`; Wiederverwendung/Anpassung von `src/components/admin/{service-form,employee-form,opening-hours-form,working-hours-board}.tsx` (ggf. nach `src/components/shared/` verschieben, um Duplikate zu vermeiden); Navigation in `layout.tsx` ergänzen.
**Prüf-/Akzeptanzkriterien:** `supabase/tests/rls_and_booking.sql` weiterhin grün; manueller Test „Salon-User kann Leistung anlegen/ändern, aber nicht `phone_number` in `voice_settings` ändern"; bestehende Admin-Funktionalität unverändert nutzbar (Admin behält vollen Zugriff); Build/TSC/Lint grün; Mobile-Check der neuen Formulare.

### Phase 5 — Erweiterter Onboarding-Assistent
**Status: 🟡 teilweise umgesetzt** im Sonderauftrag „Branchenvorlagen und Onboarding-Grundlage" (Schritte 1–3), im Sonderauftrag „Standorte, Team, Ressourcen und Verfügbarkeit" (Schritte 4–6: Standort, Öffnungszeiten, Team & Ressourcen), im Sonderauftrag „Terminarten und Leistungen (vollständig)" (Schritt 7) und, seither weiter fortgeführt, im Sonderauftrag „Eigener HalloMia-Kalender (vollständig bedienbar)" oben (Schritt 10 „Kalender testen" — Schritt 7 führt jetzt direkt dorthin). `/app/onboarding` deckt damit inzwischen sieben der zwölf Konzept-Schritte interaktiv ab (`LAST_INTERACTIVE_STEP = 10`; Schritte 8/9 „Meine Mia"/„Häufige Fragen" bleiben als inaktive Fortschritts-Vorschau dazwischen, da sie außerhalb des Kalender-Auftrags liegen). Offen bleiben die Schritte 8, 9, 11 und 12 (Meine Mia, häufige Fragen, Telefonnummer verbinden, Mia aktivieren) — sie hängen an ihrer eigenen Fachbereichs-Phase (12/13) und der Telefonie-Anbindung und werden dort ergänzt, sobald diese beauftragt werden.
**Ziel:** Schritt-für-Schritt-Einrichtung gemäß Konzept (12 Schritte), aufbauend auf Phase 3+4.
**Vorgehen:** Neuer Onboarding-Flow unter `/app/onboarding/*` mit Fortschrittsbalken und Auto-Save je Schritt (nutzt die in Phase 4 geschaffenen Self-Service-Schreibrechte). Branchenauswahl nutzt `industry_templates` aus Phase 3, inkl. „Andere Branche".
**Betroffene Dateien/DB:** neue Routen unter `src/app/app/onboarding/`; ggf. neue Spalte `salons.onboarding_completed_step` (additiv, nullable/default 0); Wiederverwendung der Formulare aus Phase 4.
**Prüf-/Akzeptanzkriterien:** Kompletter Durchlauf aller 12 Schritte auf Desktop und Smartphone ohne Datenverlust bei Unterbrechung (Reload mitten im Flow); bestehender Login-Weg für Nicht-Onboarding-Nutzer bleibt unverändert; Build/TSC/Lint grün.

### Phase 6 — Terminstatus & Buchungsquellen erweitern
**Ziel:** Status-/Quellen-Wertebereich gemäß Konzept erweitern, ohne die Doppelbuchungs-Garantie zu gefährden (Risiko 1).
**Vorgehen:** Migration, die den `check`-Constraint auf `appointments.status` erweitert (`requested, confirmed, checked_in, in_progress, completed, cancelled, no_show, callback_required` statt nur `booked/completed/cancelled/no_show`) und die Exclusion-Constraint-Bedingung von `status = 'booked'` auf die neue Menge „blockierender" Status (`confirmed, checked_in, in_progress`, ggf. `requested`, zu klären) umstellt; `source` erweitern um `import, external_calendar`. Bestehende Zeilen mit `status = 'booked'` in einem Migrationsschritt auf den neuen äquivalenten Wert (`confirmed`) ummappen.
**Betroffene Dateien/DB:** neue Migration `0016_appointment_status_expansion.sql`; `src/lib/scheduling/engine.ts`, `src/lib/actions/appointments.ts`, alle Kalender-/Listen-Komponenten, die auf `status === "booked"` prüfen (`appointment-card.tsx`, `day-view.tsx:91`, `week-grid.tsx`, `appointments-list-view.tsx`), `src/lib/voice/tools.ts`.
**Prüf-/Akzeptanzkriterien:** `tests/availability.test.ts` weiterhin grün, ggf. um neue Statuswerte ergänzt; `supabase/tests/rls_and_booking.sql`-Äquivalent für Doppelbuchung erneut manuell verifizieren (zwei parallele Buchungsversuche); bestehende Termine nach Migration weiterhin korrekt im Kalender sichtbar; Voice-Buchungsfluss (Testanruf) weiterhin erfolgreich.

### Sonderauftrag — Terminarten und Leistungen (vollständig)
**Status: ✅ umgesetzt (2026-08-28).** Auf expliziten separaten Auftrag umgesetzt, außerhalb der obigen Phasenreihenfolge und ausdrücklich beschränkt auf den Konzeptabschnitt „Flexible Terminarten" (nichts an Kalender, Telefonie, Landingpage oder anderen Bereichen wurde in diesem Schritt angefasst). Deckt Phase 4 vollständig für `services`/`employee_services`/`service_resources` ab (das dort noch offene Stück), Phase 7 vollständig (individuelle Buchungsfragen inkl. Feldtypen, Pflichtfeldsteuerung und Reihenfolge — zuvor nur als reine `jsonb`-Spalte ohne Struktur/UI vorhanden) sowie den Onboarding-Schritt 7 aus Phase 5.

**Migration** (`supabase/migrations/0033_services_self_service.sql`, additiv, angewendet auf `saloncall-ai`):
- `services.description` (Beschreibung, zuvor fehlend) und `services.has_price` (macht den Preis wie im Konzept gefordert wirklich optional — `price_cents` selbst bleibt bewusst `not null default 0`, da Buchungs-Engine, `appointment_services` und die Voice-Tools es an vielen Stellen als garantiert vorhandene Zahl lesen; `has_price = false` heißt „kein Preis hinterlegt/zu nennen", unabhängig vom rohen Zahlenwert — eine nullable Preis-Spalte hätte Kalender-/Telefonie-Code berührt, der bewusst außerhalb dieses Auftrags bleibt).
- `services`, `employee_services`, `service_resources` wechseln von „nur Plattform-Admin schreibt" auf `has_permission(salon_id, 'manage_services')` — dieselbe seit `0016_roles_permissions.sql` vorbereitete, aber bislang nirgends produktiv genutzte Berechtigung, exakt nach dem in `0031` etablierten Self-Service-RLS-Muster.
- `custom_questions` (bereits seit `0018_services_custom_questions.sql` als leere `jsonb`-Spalte vorhanden, aber nirgends gelesen/geschrieben) bekommt erstmals eine feste Struktur auf Anwendungsebene (`src/lib/validation/services.ts`): jede Frage hat `label`, einen Feldtyp (`text`, `number`, `boolean`, `select` inkl. Optionen, `date`), eine Pflichtfeldsteuerung (`required`) und eine Reihenfolge (= Position im Array, per Pfeiltasten änderbar). Kein Feldtyp und keine Beispielfrage ist global fest programmiert — Fahrzeug/Gästezahl/Haarlänge/Neubau-Altbau/Ersttermin aus dem Konzept entstehen ausschließlich durch Nutzereingabe oder, im Onboarding, durch die bereits bestehende, frei editierbare Branchenvorlagen-Vorschlagsliste (`industry_templates.example_custom_questions`).

**Self-Service-Aktionen** (`src/lib/actions/services.ts`, Muster von `team-resources.ts`): `createServiceSelfAction`, `updateServiceSelfAction`, `deleteServiceSelfAction`, `toggleServiceActiveSelfAction` — jede prüft serverseitig erneut Salon-Mitgliedschaft **und** `has_permission(salon_id, 'manage_services')`, nicht nur clientseitig ausgeblendet. **Sichere Behandlung bereits vorhandener Termine:** `appointment_services.service_id` referenziert `services` bereits seit der ursprünglichen Schema-Migration mit `on delete restrict` — der Fremdschlüssel verhindert also schon auf Datenbankebene, dass eine Terminart mit bestehenden Terminen gelöscht wird. `deleteServiceSelfAction` fängt genau diesen Fehler (Postgres-Code `23503`) ab und gibt eine verständliche Meldung mit direktem „Jetzt deaktivieren"-Link zurück, statt einer harten Exception — Deaktivieren bleibt unabhängig vom Löschen jederzeit möglich und lässt bestehende Termine unangetastet.

**Oberfläche** (`src/components/services/services-manager.tsx`, `custom-questions-editor.tsx`, wiederverwendet unter `/app/services` **und** im Onboarding): vollständiges Erstellen/Bearbeiten/Löschen sowie ein eigener Schnell-Umschalter (Aktivieren/Deaktivieren) unabhängig vom Bearbeiten-Formular. Das Formular deckt alle geforderten Felder ab: Name, Beschreibung, Dauer, optionaler Preis (Checkbox „Preis hinterlegen"), Vorbereitungs-/Nachbereitungszeit, Kalenderfarbe, Standort (Konzept: „verfügbarer Standort" — bestehendes, seit dem Datenmodell-Sonderauftrag vorhandenes einzelnes `location_id`-Feld pro Terminart, konsistent mit demselben Muster bei Mitarbeitern/Ressourcen; `null` bedeutet „an allen Standorten verfügbar"), verfügbare Mitarbeiter und benötigte Ressourcen (Mehrfachauswahl, schreibt `employee_services`/`service_resources`), telefonisch/online buchbar, aktiv/inaktiv, notwendige Kundenangaben (frei editierbare Liste über die bestehende `DraftList`-Komponente) und die individuellen Buchungsfragen über den neuen `CustomQuestionsEditor`.

Alle vier Boolean-Umschalter des Formulars (Preis hinterlegen, telefonisch/online buchbar, aktiv) senden bewusst kontrolliert ein explizites `"true"`/`"false"` über ein verstecktes Feld statt der nativen Checkbox-Semantik: Ein unangeklicktes Checkbox-Feld fehlt in `FormData` vollständig, wodurch `z.coerce.boolean().default(true)` es fälschlich wieder auf `true` gesetzt hätte — „Deaktivieren" wäre über das Bearbeiten-Formular sonst wirkungslos geblieben. `src/lib/validation/services.ts` verwendet dafür `z.enum(["true","false"]).transform(...)` statt `z.coerce.boolean()`.

**Onboarding-Schritt 7 „Terminarten & Leistungen"** (`src/components/onboarding/services-step.tsx`): nutzt dieselbe `ServicesManager`-Komponente wie `/app/services` (keine Duplikate) und bietet zusätzlich eine Schnellübernahme der in Schritt 2 gesammelten Branchenvorschläge — ein Klick auf einen Vorschlag legt eine echte Terminart an, vorbefüllt mit den zu diesem Zeitpunkt im Entwurf stehenden Buchungsfragen/Kundenangaben (alles danach frei bearbeitbar oder löschbar, wie alle Onboarding-Vorschläge). `LAST_INTERACTIVE_STEP` in `src/lib/onboarding/steps.ts` von 6 auf 7 angehoben; Schritt 6 „Team & Ressourcen" führt jetzt zu Schritt 7 weiter statt direkt zum Dashboard.

**Bewusst nicht in diesem Schritt umgesetzt:** keine Anbindung der Buchungsfragen an den Sprachagent-Prompt oder die Voice-Tool-Schemas (das ist eigene Telefonie-Arbeit, ursprünglich als Teil von Phase 7 vorgesehen, aber laut Auftrag nicht Teil von „Terminarten und Leistungen" — die Daten liegen jetzt strukturiert vor und sind dafür bereit); keine Änderung an Admin-Formular/-Aktionen (`src/components/admin/service-form.tsx`, `src/lib/actions/admin.ts`) — Plattform-Admins nutzen weiterhin ihren bestehenden, schlankeren Weg und funktionieren unverändert weiter (`has_permission()` lässt sie ohnehin immer zu, da `is_platform_admin()` darin immer Vorrang hat); keine Migration bestehender Custom-Questions-Daten nötig, da die Spalte zuvor nirgends beschrieben wurde (immer `[]`).

**Geprüft:** `npx tsc --noEmit`, `npx eslint .` (0 Fehler, dieselbe vorbestehende Warnung wie in Phase 1) und `npm run build` (alle 41 Routen) fehlerfrei; `npx vitest run` weiterhin grün (20/20). `get_advisors` (Supabase, `security`) zeigt keine neuen Befunde zu `services`/`employee_services`/`service_resources` — alle gemeldeten Warnungen sind vorbestehend und unabhängig von dieser Änderung.

### Phase 7 — Flexible Terminarten: individuelle Buchungsfragen
**Status: 🟡 teilweise umgesetzt** im Sonderauftrag „Terminarten und Leistungen (vollständig)" oben: Datenmodell (Feldtypen, Pflichtfeldsteuerung, Reihenfolge) und vollständige Self-Service-UI sind fertig. Offen bleibt ausdrücklich die Anbindung an den Sprachagent-Prompt und die Voice-Tool-Schemas, damit Mia die Fragen im Telefonat tatsächlich stellt — das ist eigene Telefonie-Arbeit und war nicht Teil des Auftrags „Terminarten und Leistungen".
**Ziel:** Konzept-Anforderung „individuelle Buchungsfragen pro Terminart".
**Vorgehen:** Neue Spalte `services.custom_questions jsonb default '[]'`; Formular-Erweiterung in `service-form.tsx`; Übergabe der Fragen an den Voice-Prompt (`src/lib/voice/prompt.ts`) und an die Tool-Schemas (`src/lib/voice/tool-json-schemas.ts`), sodass die KI sie bei Buchung abfragt und die Antworten in `appointments.notes` oder einer neuen `appointment_answers`-Tabelle ablegt.
**Betroffene Dateien/DB:** Migration `0017_service_custom_questions.sql`; `src/components/admin/service-form.tsx` (bzw. das in Phase 4 self-service-fähige Pendant); `src/lib/voice/{prompt,tools,tool-json-schemas}.ts`; `new-appointment-modal.tsx` (manuelle Buchung im Kalender soll dieselben Fragen zeigen).
**Prüf-/Akzeptanzkriterien:** Testanruf (OpenAI Voice Test Mode) stellt die konfigurierte Frage und speichert die Antwort sichtbar am Termin; manuelle Buchung über den Kalender zeigt dieselben Felder; bestehende Terminarten ohne Fragen funktionieren unverändert (leeres Array als Default).

### Phase 8 — Anfragen & Rückrufe als eigenständige Domäne
**Status: 🟡 nur Grundlage vorbereitet.** Das Datenmodell (`requests`-Tabelle inkl. verlustfreier Übernahme der bestehenden `callback_requests`-Zeilen als `category = 'callback'`) existiert bereits seit dem Sonderauftrag „Datenmodell, Mandantenfähigkeit und Berechtigungen"; im Rahmen von Phase 9 wurde zusätzlich Migration `0034` angewendet (Schreibrechte auf `has_permission('manage_requests')` verschärft, privater Storage-Bucket `request-attachments` angelegt). **Weiterhin offen und nicht Teil dieses Schritts:** `src/lib/actions/requests.ts`, eine neue `/app/requests`-Oberfläche auf Basis von `requests` (Kategorie, Dringlichkeit, Anhänge, zuständiger Mitarbeiter) statt der bisherigen, weiterhin unveränderten `callback_requests`-Ansicht, sowie das `createRequest`-Voice-Tool. Sollte als eigener Auftrag vom Umfang etwa einer der bisherigen Sonderaufträge behandelt werden.
**Ziel:** Konzept-Bereich „Anfragen" (nicht jeder Kontakt führt zu einem festen Termin).
**Vorgehen:** Neue Tabelle `requests` (Kunde, Anliegen, Kategorie, Beschreibung, Rückrufzeitraum, Dringlichkeit, zuständiger Mitarbeiter, Status gemäß Konzept, interne Notizen), ergänzt `callback_requests` (bleibt als Spezialfall/Verweis bestehen oder wird zu einer Sicht auf `requests` mit Kategorie „Rückruf"). Neue Navigation „Anfragen" im Salon-Portal.
**Betroffene Dateien/DB:** Migration `0018_requests.sql` inkl. RLS-Policies nach bestehendem Muster; neue Seite `src/app/app/(portal)/requests/page.tsx`; neue Komponenten `src/components/requests/*`; Erweiterung `src/lib/voice/tools.ts` um ein `createRequest`-Tool für die KI.
**Prüf-/Akzeptanzkriterien:** Bestehende `callback_requests`-Funktionalität (Anzeige unter „Anrufe") bleibt unverändert nutzbar oder wird sauber migriert (keine Datenverluste — vor Migration Zeilenanzahl prüfen, nach Migration vergleichen); neue Anfragen lassen sich anlegen/bearbeiten/status-wechseln; RLS-Test für Tenant-Isolation der neuen Tabelle.

### Phase 9 — Kundenverwaltung erweitern
**Status: ✅ umgesetzt (2026-08-28).** Datenmodell (`status`, `tags`, `custom_fields`, `address`, `company`, `consent_recording`, `consent_marketing`, `deleted_at`) existierte bereits additiv seit dem Sonderauftrag „Datenmodell, Mandantenfähigkeit und Berechtigungen" (Migration `0017_customers_extended.sql`), ebenso das generische `custom_field_definitions`-Framework (`0024`) und die DSGVO-RPCs `export_customer_data`/`delete_customer_data` (`0026_gdpr.sql`) — bislang jeweils ohne Anwendungscode. Dieser Schritt liefert die fehlende Verdrahtung nach, exakt nach dem in `services.ts`/`ServicesManager` etablierten Self-Service-Muster:

- `src/lib/validation/customers.ts` (neu): Status-Enum mit Labels/Badge-Farben, `customerSelfSchema` inkl. der expliziten `"true"/"false"`-Checkbox-Konvention (siehe `validation/services.ts`), `tagsSchema`.
- `src/lib/actions/customers.ts` (überarbeitet — war zuvor das schlanke, ungeschützte Grundformular): `requireCustomersAccess()` prüft `has_permission(salonId, 'manage_customers')` zusätzlich zur bestehenden `is_salon_member`-RLS auf Anwendungsebene (RLS selbst bewusst **nicht** verschärft, da `createManualAppointmentAction` beim Kalender-Buchen auch für die Rolle „Nur Kalenderzugriff" neue Kunden anlegen muss). `createCustomerAction`/`updateCustomerAction` schreiben jetzt alle neuen Felder, lösen individuelle Felder über `parseCustomFieldValues` (gegen serverseitig frisch geladene `custom_field_definitions`, nicht gegen Client-Eingaben) auf und geben bei einer per `findDuplicateCustomers` (`src/lib/customers/dedupe.ts`, Telefon **und** E-Mail, normalisiert) erkannten Dublette einen nicht-blockierenden Hinweis zurück (`{ duplicates }`), der erst nach explizitem zweitem Klick „Trotzdem anlegen/speichern" (natives Submit-Button-`name="confirm_duplicate"`, kein Rennen mit React-State) übergangen wird. Neu: `deleteCustomerDataAction`/`exportCustomerDataAction` als dünne Wrapper um die beiden DSGVO-RPCs.
- Diese Aktionen sind gemeinsame Basis für **beide** Oberflächen (`/app/customers` **und** `/admin/salons/[salonId]/customers`, die dieselben `CustomersView`/`CustomerForm`-Komponenten rendern) — `has_permission()` lässt Plattform-Admins wie gehabt immer zu, `resolveActiveSalonId(session, salonId)` musste dafür explizit mit der Ziel-`salonId` aufgerufen werden (ohne das zweite Argument liefert die Funktion für einen Admin ohne eigene Salon-Mitgliedschaft `null` zurück und hätte die Admin-Kundenverwaltung fälschlich blockiert — beim Kopieren des `services.ts`-Musters gefunden und korrigiert, dort unschädlich, weil Admin-Kunden dort einen komplett getrennten Code-Pfad nutzen).
- Oberfläche (`customer-form.tsx`, `customer-profile-view.tsx`, `customers-view.tsx`, neu `customer-gdpr-actions.tsx`): Status-Badge, Tags (Chip-Editor), Firma/Adresse, zwei Einwilligungs-Checkboxen, individuelle Felder über die bereits vorhandenen, bislang ungenutzten `CustomFieldValueInputs`/`CustomFieldDefinitionsManager`-Komponenten (`src/components/shared/*`), Dubletten-Hinweis, sowie ein „Datenschutz"-Kartenbereich mit Datenexport (JSON-Download) und DSGVO-Löschung. Kundenliste zeigt Status-Badge und Tags, blendet anonymisierte (`deleted_at is not null`) Datensätze aus.
- Neuer Bereich „Individuelle Kundenfelder" unter `/app/settings` (`CustomFieldDefinitionsManager` für `entityType="customer"`), sichtbar für jede Rolle, bearbeitbar nur mit `manage_settings`.
- Nebenbei behoben: `customer-form.tsx` schloss das Bearbeiten-/Anlegen-Modal bislang nach **jedem** Submit sofort (auch bei Validierungsfehlern) — dadurch waren Fehlermeldungen faktisch nie sichtbar. Jetzt schließt das Modal nur noch bei echtem `ok: true` (`useEffect` auf den Aktionsstatus statt eines unbedingten Callbacks direkt nach dem Submit).

**Bewusst nicht in diesem Schritt umgesetzt:** keine Migration nötig (Datenmodell war bereits vollständig vorhanden); keine Änderung an admin-spezifischem Code, da Admin und Salon-Portal dieselben Komponenten/Aktionen teilen; keine Anbindung von `custom_field_definitions` für `entityType="request"` an eine Oberfläche (Anfragen-Domäne bleibt Phase 8, siehe unten).

**Geprüft:** `npx tsc --noEmit`, `npx eslint .` (0 Fehler, dieselbe vorbestehende Warnung wie in Phase 1) und `npm run build` (alle 41 Routen) fehlerfrei; `npx vitest run` weiterhin grün (31/31, unverändert). Kein browserbasierter Klick-Durchlauf in dieser Sitzung (dieselbe Sandbox-Netzwerkeinschränkung wie in den vorherigen Phasen dokumentiert) — stattdessen Code-Review gegen das bereits live verifizierte `services.ts`-Muster und gezielte Prüfung der Admin/Salon-Rechte-Verzweigung (`resolveActiveSalonId`-Fix oben).

**Zusätzlich, kleinerer Randauftrag:** Migration `0034_requests_permission_rls_and_attachments.sql` (bereits vor dieser Sitzung vorbereitet, aber nicht angewendet) wurde auf das Live-Projekt angewendet — verschärft die Schreibrechte auf der seit dem Datenmodell-Sonderauftrag bestehenden, aber noch ungenutzten `requests`-Tabelle von `is_salon_member` auf `has_permission('manage_requests')` und legt den privaten Storage-Bucket `request-attachments` für künftige Anfragen-Anhänge an. Gefahrlos, da noch keine Anwendung in diese Tabelle schreibt. Die eigentliche Anfragen-Oberfläche (`src/lib/actions/requests.ts`, neue `/app/requests`-Seite auf Basis von `requests` statt `callback_requests`, Kategorie/Dringlichkeit/Anhänge) ist **nicht** Teil dieses Schritts — das bleibt vollständig Phase 8 und sollte als eigener, ähnlich großer Auftrag behandelt werden wie dieser hier.

### Phase 10 — Standorte (Locations)
**Status: ✅ umgesetzt** im Sonderauftrag „Standorte, Team, Ressourcen und Verfügbarkeit" oben (Datenmodell bereits zuvor im Sonderauftrag „Datenmodell, Mandantenfähigkeit und Berechtigungen", jetzt mit echter Self-Service-Verwaltung unter `/app/locations`). Der hier vorgeschlagene Standort-Filter im Kalender selbst ist bewusst nicht Teil dieses Auftrags (Kalender-Arbeit, siehe Phase 14).
**Ziel:** Ein Unternehmen kann mehrere Standorte haben (Konzept: „Standort optional" bei Terminen, Filter nach Standort im Kalender).
**Vorgehen:** Neue Tabelle `locations` (salon_id, Name, Adresse, Öffnungszeiten-Override); nullable `location_id` auf `employees`, `services`, `appointments`. Bestehende Salons erhalten automatisch einen impliziten Default-Standort (Datenmigration), damit nichts bricht, wenn `location_id` vorerst überall `null`/Default bleibt.
**Betroffene Dateien/DB:** Migration `0020_locations.sql`; `src/components/calendar/employee-filter.tsx` um Standort-Filter ergänzen (bzw. neue `location-filter.tsx`); `calendar-shell.tsx`, `calendar-data.ts`.
**Prüf-/Akzeptanzkriterien:** Bestehende Ein-Standort-Salons funktionieren unverändert (impliziter Default-Standort); neuer Test-Salon mit zwei Standorten zeigt korrekt gefilterte Kalenderdaten; RLS/Tenant-Isolation weiterhin geprüft.

### Phase 11 — Generische Ressourcen (Räume, Fahrzeuge, Geräte, Tische)
**Status: 🟡 teilweise umgesetzt** im Sonderauftrag „Standorte, Team, Ressourcen und Verfügbarkeit" oben: Self-Service-Verwaltung (`/app/team`) und die hier geforderte Verfügbarkeitsprüfung sind fertig — `computeFreeSlots`/`checkAvailability` berücksichtigen eine übergebene Ressource (Arbeitszeiten, Sperrung, bestehende Buchungen), additiv und getestet. Offen bleibt der als „aufwendigster Teil" markierte Rest: eine Terminart verpflichtend an Ressourcen koppeln, automatische Ressourcenauswahl bei der Buchung, Ressourcen-Spalten/-Filter im Kalender selbst.
**Ziel:** Konzept-Anforderung „buchbare Ressourcen" jenseits von Mitarbeitern.
**Vorgehen:** Neue Tabelle `resources` (Name, Typ, Standort, Verfügbarkeitszeiten, zugewiesene Terminarten, Kalenderfarbe, aktiv/inaktiv) + `appointment_resources`-Verknüpfungstabelle. Verfügbarkeitsprüfung (`src/lib/scheduling/availability.ts`) muss erweitert werden, sodass ein Termin nur angeboten wird, wenn **sowohl** Mitarbeiter **als auch** benötigte Ressourcen frei sind — dies ist der aufwendigste fachliche Teil des gesamten Plans und sollte erst nach Phase 6 (stabiler Status-Wertebereich) und Phase 10 (Standorte) erfolgen.
**Betroffene Dateien/DB:** Migration `0021_resources.sql`; `src/lib/scheduling/availability.ts`, `engine.ts` (neue Parameter, bestehende Signatur möglichst additiv erweitern, nicht brechen); `tests/availability.test.ts` um Ressourcen-Fälle ergänzen; Kalender-UI um Ressourcen-Spalten/-Filter erweitern.
**Prüf-/Akzeptanzkriterien:** Alle bestehenden `tests/availability.test.ts`-Fälle (ohne Ressourcen) weiterhin grün; neue Testfälle für „Ressource belegt → kein Slot" grün; manueller Doppelbuchungstest für Ressourcen analog zum bestehenden Mitarbeiter-Test.

### Phase 12 — Meine Mia: erweiterte KI-Konfiguration & Testchat
**Status: ✅ umgesetzt (2026-08-28).** Auf expliziten separaten Auftrag umgesetzt: „Lies die beiden HalloMia-Dokumente vollständig und setze ausschließlich den Bereich „Meine Mia" um." Deckt den kompletten Konzeptabschnitt „KI-Assistent konfigurieren" ab, einschließlich Testfunktion und der zugehörigen Onboarding-Schritte 8–9. Kein anderer Konzeptbereich wurde in diesem Schritt angefasst.

**Migration `0035_meine_mia_config.sql`:** `salons.description` (Unternehmensbeschreibung) neu; die beiden bestehenden Self-Service-RPCs erweitert, statt die RLS-Vertrauensgrenze aus Risiko 2 aufzuweichen — `update_voice_settings_customer_fields` um die in der Sonderauftrags-Migration `0021` bereits als reine Spalten angelegten Felder (`assistant_name`, `formality`, `languages`, `never_mention`, `after_hours_behavior`, `handoff_number`, `urgent_keywords`, `notify_after_call`), `update_own_salon_onboarding` um `p_description`. Beide Erweiterungen folgen exakt dem bestehenden Drop-and-recreate-Muster aus `0011`/`0029` (Postgres behandelt ein geändertes Parameter-Set als neuen Overload).

**„Meine Mia"-Formular (`/app/ai`, `ai-settings-form.tsx`):** vollständig um Assistentinnenname, Du/Sie-Ansprache, Hauptsprache + weitere Sprachen (kommagetrennte Codes), Unternehmensbeschreibung, „darf niemals nennen", Verhalten außerhalb der Öffnungszeiten (Rückruf/Nachricht/nur Infos), Weiterleitungsnummer bei dringenden Fällen, Anzeichen für dringende Fälle und die Benachrichtigung-nach-Gespräch-Option ergänzt — alle bereits bestehenden Felder (Begrüßung, Tonalität, Regeln, Stornofrist, Mitzubringendes) unverändert erhalten.

**FAQ:** Die bereits seit `0020_faq.sql` bestehende, self-service-schreibbare `faq`-Tabelle war bisher an keiner Oberfläche angebunden — neue `FaqManager`-Komponente (`src/components/portal/faq-manager.tsx`, `src/lib/actions/faq.ts`) auf `/app/ai` sowie ein neues `getFaq`-Voice-Tool (`src/lib/voice/tools.ts`, `tool-json-schemas.ts`), damit Mia hinterlegte Fragen am Telefon tatsächlich direkt beantwortet statt zu raten.

**Grundregeln technisch umgesetzt (`src/lib/voice/prompt.ts`):** über die bereits bestehende Tool-erzwungene Grundarchitektur hinaus (Preise/Öffnungszeiten/Verfügbarkeiten kommen ausschließlich aus echten Tool-Aufrufen, nie aus Modellwissen) wurden die im Konzept explizit geforderten, bisher nur implizit geltenden Regeln als klare Prompt-Regeln ergänzt: Preise nur nennen, wenn `getServices` sie als hinterlegt zurückgibt (dazu `has_price`-Fix in `getServices`, das zuvor `price_cents / 100` unabhängig vom „Preis optional"-Flag zurückgab); vor `createAppointment`/`rescheduleAppointment`/`cancelAppointment` alle Termindaten zusammenfassen und ausdrücklich bestätigen lassen; keine erfundenen medizinischen/rechtlichen/finanziellen Aussagen; Verhalten außerhalb der Öffnungszeiten (neu: aktuelle Uhrzeit im Prompt-Header, zuvor nur Datum/Wochentag); Weiterleitung/dringende Fälle anhand `urgent_keywords`/`handoff_number`; Du/Sie jetzt aus `formality` statt fest codiertem „du"; Hauptsprache + Sprachwechsel bei unterstützten weiteren Sprachen. `VoiceAgentConfig`, `build-config.ts` und der tägliche Retell-Resync-Cron (`resync-voice-agents/route.ts`, baute `VoiceAgentConfig` bisher eigenständig auf) wurden konsistent um alle neuen Felder erweitert, damit Retell- und ElevenLabs-Sync nicht auseinanderlaufen (Risiko 7).

**Testfunktion (`/app/ai/test`):** Kein Ausbau einer vorbestehenden Funktion — außer dem Umgebungsvariablen-Eintrag „OpenAI API Key (Voice Test Mode)" im Admin-System-Check existierte keine Testanruf-Logik im Projekt. Neu gebaut: `sendTestChatMessageAction` (`src/lib/actions/ai-test-chat.ts`) nutzt exakt `buildPromptFromConfig` mit den tatsächlich gespeicherten `voice_settings` dieses Unternehmens (keine separate Test-Prompt-Logik) und die OpenAI Chat-Completions-API mit denselben Tool-Definitionen wie die echten Voice-Provider (`toolJsonSchemas`/`toolDescriptions`, Modell konfigurierbar über `OPENAI_TEST_CHAT_MODEL`, Default `gpt-4o-mini`). Lesende Tools (Öffnungszeiten, Leistungen, FAQ, Verfügbarkeit, Kundensuche) laufen live gegen die echte, RLS-geschützte Datenbank — Verfügbarkeiten werden also tatsächlich über die zentrale Kalenderlogik geprüft, nicht simuliert. Schreibende Tools (`createAppointment`, `rescheduleAppointment`, `cancelAppointment`, `createCustomer`, `createCallbackRequest`) werden bewusst **nie** ausgeführt, sondern nur als „simuliert" an das Modell zurückgemeldet — der Testchat kann dadurch unter keinen Umständen einen echten Termin, Kunden oder Rückruf erzeugen, auch nicht nach einer im Gespräch ausgesprochenen Bestätigung. Die Oberfläche (`ai-test-chat.tsx`) zeigt durchgehend einen Simulationshinweis sowie nach jeder Antwort, welche Aktionen simuliert wurden.

**Onboarding-Schritte 8–9:** die bisher als „Folgt später" geführten, nicht anklickbaren Vorschau-Schritte in `src/lib/onboarding/steps.ts` sind jetzt interaktiv. Schritt 8 „Begrüßung & Tonalität" (`greeting-step.tsx`, `saveGreetingStepAction`) deckt Assistentinnenname, Begrüßung, Tonalität, Du/Sie und Unternehmensbeschreibung ab — die Aktion lädt die bestehende `voice_settings`-Zeile und reicht alle übrigen, im Onboarding nicht gezeigten Felder unverändert an die RPC durch (die Self-Service-RPC erwartet weiterhin den vollständigen Feldsatz, siehe Risiko 2). Schritt 9 „Häufige Fragen" (`faq-step.tsx`) bettet denselben `FaqManager` wie unter „Meine Mia" ein. `LAST_INTERACTIVE_STEP` bleibt 10 (Kalendertest); ein Unternehmen, dessen `onboarding_step` vor diesem Schritt bereits 10 erreicht hatte (unter dem alten Ablauf, der 8/9 übersprang), landet beim nächsten Aufruf direkt auf dem Kalendertest und sieht 8/9 nicht rückwirkend — akzeptierter Rand für die wenigen bereits laufenden Onboarding-Sitzungen.

**Bewusst nicht in diesem Schritt umgesetzt:** eine echte Anruf-Weiterleitung an `handoff_number` beim Telefonanbieter selbst (Retell/ElevenLabs „Transfer Call") — das ist Telefonie-Integrationsarbeit für eine spätere Phase; hier wird die Nummer nur im Prompt als anzubietende Weiterleitung genannt und in `createCallbackRequest` als dringender Rückruf erfasst. Kein Versand-Kanal für `notify_after_call` (E-Mail/SMS/Push) — die bereits bestehende `notifications`/`notification_preferences`-Infrastruktur aus dem Datenmodell-Sonderauftrag bleibt Phase 17.

**Geprüft:** `npx tsc --noEmit`, `npx eslint .` (0 Fehler, dieselbe vorbestehende Warnung wie in den früheren Phasen) und `npm run build` (alle 40 Routen, neu: `/app/ai/test`) fehlerfrei; `npx vitest run` weiterhin grün (31/31). Migration auf das Live-Projekt `saloncall-ai` angewendet, TypeScript-Typen neu generiert, `get_advisors` zeigt keine neuen kritischen Befunde (dieselben vorbestehenden WARN-Einträge zum etablierten `SECURITY DEFINER`-Self-Service-Muster wie bei allen bisherigen RPCs dieser Art).

### Phase 13 — Rollen & Berechtigungen ausbauen
**Ziel:** Konzept-Rollen (Inhaber, Administrator, Mitarbeiter, Empfang, Nur Kalenderzugriff) inkl. „Mitarbeiter sieht nur eigene Termine".
**Vorgehen:** `salon_users.role`-Check-Constraint erweitern; RLS-Policies für `appointments`/`customers` um eine Zeilenfilterung „eigene Termine" ergänzen, wenn Rolle = Mitarbeiter (Vorbild: bestehende `is_salon_member()`-Funktion um eine `is_own_appointment()`-Variante ergänzen); UI-Rechteprüfung in den Server Actions ergänzen (nicht nur clientseitig ausblenden).
**Betroffene Dateien/DB:** Migration `0023_roles.sql`; `src/lib/auth/session.ts`; diverse `src/lib/actions/*.ts` (serverseitige Rechteprüfung).
**Prüf-/Akzeptanzkriterien:** Test je Rolle: Mitarbeiter-Login sieht nur eigene Termine/Kunden, Empfang sieht alles außer Einstellungen, Nur-Kalenderzugriff kann keine Kunden-Stammdaten öffnen; bestehende `owner`/`staff`-Logins verhalten sich weiterhin wie „Inhaber"/„Mitarbeiter" (Abwärtskompatibilität der bestehenden zwei Rollen sicherstellen).

### Phase 14 — Kalender-Feinschliff (Suche, weitere Filter, Resize)
**Status: ✅ umgesetzt** im Sonderauftrag „Eigener HalloMia-Kalender (vollständig bedienbar)" oben — Suche, Filter nach Standort/Terminart/Status (kombinierbar mit dem bestehenden Mitarbeiterfilter) und Resize-Handles in Tages-/Wochenansicht. Touch-Resize ist auf Smartphones implizit deaktiviert, ohne eine Sonderfall-Abschaltung zu brauchen: die mobile Ansicht zeigt ohnehin die eigenständige `MobileAgenda`-Kartenliste statt der Drag/Resize-fähigen Grid-Ansichten (siehe 1.11), passend zum Antipattern „keine überladenen Ansichten".
**Ziel:** Restliche Konzept-Anforderungen an den Kalender (Suche, Filter nach Terminart/Status, Verlängern/Verkürzen per Drag).
**Vorgehen:** Erweiterung von `calendar-shell.tsx`/`employee-filter.tsx` um Such-/Terminart-/Status-Filter (client- oder serverseitig, je nach Datenmenge); Resize-Handles in `appointment-card.tsx`/`day-view.tsx` ergänzend zum bestehenden Drag-to-Move.
**Betroffene Dateien/DB:** keine DB-Änderung nötig (nutzt vorhandene/durch Phase 6–11 erweiterte Felder); `src/components/calendar/*`.
**Prüf-/Akzeptanzkriterien:** Bestehendes Drag-to-Reschedule weiterhin funktionsfähig (Regressionstest); neue Filter kombinierbar mit Mitarbeiterfilter; Mobile-Check (Touch-Resize ist auf kleinen Screens ggf. bewusst zu deaktivieren zugunsten der Detail-Ansicht pro Antipattern „keine überladenen Ansichten").

### Phase 15 — Multi-Branchen-Demodaten
**Ziel:** Konzept-Vorgabe „mehrere Demo-Unternehmen, je eigene Branche, nicht vermischt".
**Vorgehen:** Zusätzliche, unabhängig ausführbare Seed-Skripte (`supabase/seed-handwerk.sql`, `seed-arztpraxis.sql`, `seed-restaurant.sql` o. ä. bzw. Erweiterung von `scripts/seed.ts`), die **parallel** zum bestehenden `seed.sql` existieren, ohne es zu verändern oder zu ersetzen.
**Betroffene Dateien/DB:** neue Seed-Dateien; keine Migrationen.
**Prüf-/Akzeptanzkriterien:** Bestehender `seed.sql`-Ablauf weiterhin unverändert lauffähig; neue Seeds erzeugen jeweils ein sauber isoliertes Demo-Unternehmen (RLS-Test: Demo-Unternehmen A sieht keine Daten von Demo-Unternehmen B).

### Phase 16 (optional, niedrige Priorität) — Interne Umbenennung „salon" → „company"/„Unternehmen"
**Ziel:** Auch interne Bezeichner (DB-Spalten, Typnamen, Routen) generisch benennen.
**Empfehlung:** **Nicht zwingend nötig**, um die Akzeptanzkriterien des Konzepts zu erfüllen (diese verlangen sichtbar generische Begriffe im Produkt, siehe Abschnitt 1.8). Aufgrund des hohen Risikos (Risiko 8: 794 Fundstellen, RLS-Funktionen, Realtime-Publikation, Webhook-URLs, Storage-Policies) wird empfohlen, diesen Schritt **nur bei ausdrücklichem separatem Auftrag** und mit einer eigenen, sehr kleinteiligen Sub-Planung (Spalten-Alias/View-basierte Übergangsphase statt Big-Bang-Rename) anzugehen.
**Falls beauftragt — Vorgehen:** schrittweise über SQL-Views/Alias-Spalten, Anwendungscode zuerst auf die neuen Namen umstellen, alte Namen erst nach vollständiger Verifikation entfernen.
**Prüf-/Akzeptanzkriterien:** vollständiger Regressionslauf aller vorherigen Phasen-Akzeptanzkriterien nach dem Rename.

### Phase 17 — Statistiken & Benachrichtigungen ausbauen
**Ziel:** Konzept-Bereiche „Statistiken" und „Benachrichtigungen" vollständig.
**Vorgehen:** Auswertungs-Queries/Views für Buchungsquote, Stornoquote, Rückrufquote, Auslastung je Mitarbeiter, häufigste Anliegen (Auswertung von `calls.topic`/`requests.category`); Benachrichtigungsmatrix (Ereignis × Kanal) auf Basis der in Phase 8 geschaffenen `requests`-Tabelle und der bestehenden `src/app/api/notifications/route.ts`.
**Betroffene Dateien/DB:** ggf. Migration für aggregierende Views; `src/app/app/(portal)/dashboard/page.tsx`, neue Statistik-Seite.
**Prüf-/Akzeptanzkriterien:** Zahlen stimmen mit manueller Kontrollabfrage überein; keine erfundenen Live-Daten (Konzept-Vorgabe) — bei fehlenden Daten explizite „Noch keine Daten"-Anzeige statt Platzhalterzahlen.

### Sonderauftrag — Anrufe, Gespräche und Telefonie-Schnittstelle
**Status: ✅ umgesetzt (2026-08-28).** Auf expliziten separaten Auftrag umgesetzt, außerhalb der obigen Phasenreihenfolge und ausdrücklich beschränkt auf die Konzeptabschnitte „Gesprächsübersicht" und „Telefonie und Integrationen". Baut auf den seit dem Sonderauftrag „Datenmodell, Mandantenfähigkeit und Berechtigungen" vorhandenen, bis dahin ungenutzten `calls`-Spalten (`summary`, `urgency`, `sentiment`, `resolved`, `consent_recording`, `recording_url`) auf und macht sie erstmals produktiv.

**Neue Migrationen** (`supabase/migrations/0036`–`0038`, additiv, angewendet auf `saloncall-ai`):
- `calls.notes` (interne Notizen) sowie Indizes für `callback_requests.call_id`/`calls.provider_call_id`.
- `voice_settings.recording_enabled` (Default `false`) + eigene `SECURITY DEFINER`-RPC `update_call_recording_consent` — rechtliche Aktivierung der Audioaufzeichnung durch das Unternehmen selbst, nach dem etablierten Muster enger RPCs statt einer breiten `UPDATE`-Policy; wird nie automatisch aktiviert.
- `voice_settings.twilio_phone_number_sid` — dritter, unabhängig synchronisierbarer Provider nach demselben Muster wie `elevenlabs_agent_id`.
- `complete_own_salon_onboarding()` — schließt den zwölfstufigen Einrichtungsassistenten ab (setzt `salons.onboarding_completed_at`).

**Providerunabhängige Webhook-Schnittstelle** (`src/lib/voice/call-ingest.ts`): zentrale, von allen drei Providern (Retell, ElevenLabs, Twilio) genutzte Funktionen (`ensureCall`, `finalizeCall`, `attachRecording`, `linkCallAppointment`, `linkCallCallback`, `markCallHandoff`) decken exakt die im Konzept genannten Ereignisse ab (eingehender Anruf, Gespräch gestartet/beendet, Transkript/Zusammenfassung verfügbar, Termin angefragt/gebucht, Rückruf erstellt, Weiterleitung ausgelöst) und garantieren wiederholbare Verarbeitung durch Lookup-vor-Schreiben nach `provider_call_id` (kein Provider erzeugt mehr Duplikate bei mehrfachen Lifecycle-Events pro Gespräch). `src/app/api/voice/webhook/route.ts` (Retell) und `.../elevenlabs/**` nutzen dieses Modul jetzt für Anruf-Verknüpfung (Termin/Rückruf ↔ Gespräch) und werten erstmals Retells `call_analysis`/ElevenLabs' Post-Call-Analyse aus (Zusammenfassung, Stimmung, aus `urgent_keywords` abgeleitete Dringlichkeit) — bislang blieben `summary`/`urgency`/`sentiment`/`appointment_id` bei jedem Anruf leer.

**Twilio als dritter Provider** (`src/lib/voice/providers/twilio.ts`, `src/app/api/voice/webhook/twilio/{voice,events}/route.ts`, Sync-Panel in `/admin/salons/[salonId]/ai`): Twilio ist ein reiner Telefonie-Träger ohne eigene Konversations-KI — `syncAgent` verknüpft die hinterlegte Telefonnummer im Twilio-Konto mit HalloMias eigenen Webhooks, statt (wie bei Retell/ElevenLabs) einen Sprachagenten anzulegen. Der `voice`-Webhook führt stattdessen selbst ein ehrliches, kleines IVR (Begrüßung, Rückrufaufnahme, Weiterleitung an eine hinterlegte Nummer) statt ein KI-Gespräch vorzutäuschen; für ein echtes KI-Gespräch bleibt Retell oder ElevenLabs nötig. Signaturprüfung folgt Twilios eigenem, vollständig spezifiziertem Verfahren (`X-Twilio-Signature`, HMAC-SHA1 über URL + sortierte POST-Parameter) statt eines selbst erfundenen Shared Secrets wie bei Retell/ElevenLabs. Zugangsdaten (`TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`) bleiben ausschließlich serverseitig (dieselben Variablen wie die bereits bestehende SMS-Integration).

**Gesprächsübersicht** (`/app/calls`, `src/components/calls/{calls-view,calls-list,recording-consent-card}.tsx`, `src/lib/calls/filters.ts`): zeigt jetzt alle im Konzept geforderten Felder (Datum/Uhrzeit, Name/Telefonnummer, Dauer, Zusammenfassung, Anliegen, vereinbarter Termin, Rückrufwunsch, Status, Dringlichkeit, Audioaufnahme sofern vorhanden, Transkript, interne Notizen) sowie alle sechs geforderten Filter (Termin gebucht, Rückruf erforderlich, nicht gelöst, dringend, Bestandskunde, Neukunde) als kombinierbare Chips — reine Filterlogik in `matchesCallFilters()` ist unit-getestet (`tests/calls-filters.test.ts`). Interne Notizen sind direkt in der Übersicht editierbar (nutzt die bereits bestehende `calls_update`-RLS-Policy). Eine eigene Karte macht die rechtliche Aktivierung der Audioaufzeichnung sichtbar und self-service bedienbar, kommt aber nie vorausgewählt an. Ohne verbundene Telefonnummer zeigt die Seite einen klar gekennzeichneten Demo-Modus-Hinweis statt so zu wirken, als kämen bereits echte Anrufe an.

**Onboarding-Schritte 11/12** (`src/components/onboarding/{phone-step,activate-step}.tsx`): Schritt 11 „Telefonnummer verbinden" zeigt den echten, aus `voice_settings` gelesenen Verbindungsstatus (verbunden/nicht verbunden, mit Anbieter) statt eines vorgetäuschten Selbstverbindungs-Formulars — eine echte Nummer einzurichten bleibt bewusst an das HalloMia-Team gebunden (`phone_number`/`provider_*` sind seit dem Datenmodell-Sonderauftrag absichtlich Admin-only, siehe RLS 0002/Risiko 2), dafür ein direkter Kontakt-Link. Schritt 12 „Mia aktivieren" nutzt den bestehenden `AiToggle`/`toggle_salon_ai`-Pfad und schließt den Assistenten über `complete_own_salon_onboarding()` ab; `LAST_INTERACTIVE_STEP` von 10 auf 12 angehoben, alle zwölf Konzept-Schritte sind jetzt interaktiv.

**Bewusst nicht in diesem Schritt umgesetzt** (jeweils mit Begründung):
- Kein echtes KI-Gespräch auf Twilio (siehe oben) — das bleibt Retell/ElevenLabs vorbehalten, eine eigene Sprach-KI wäre eine unautorisierte Ausweitung auf den bereits bestehenden Voice-Prompt/Tool-Bereich.
- Keine Änderung an `src/lib/voice/{prompt,tools,tool-json-schemas}.ts` oder am Sprachagent-Prompt selbst — nur die Webhook-/Ingestion-Schicht wurde erweitert, die KI verspricht dadurch nichts Neues.
- Die branchenneutrale `requests`-Tabelle (Phase 8) bleibt unangetastet; Rückrufwunsch-Verknüpfung nutzt weiterhin ausschließlich `callback_requests`, wie es die bestehende Gesprächsübersicht bereits tat.
- Kein Dashboard-Status „Mia ist aktiv/pausiert/Telefonnummer nicht verbunden" (Konzeptabschnitt „Dashboard", nicht „Anrufe/Telefonie") — der Demo-Modus-Hinweis lebt bewusst nur auf der Anrufe-Seite.
- Kein täglicher Cron-Resync für Twilio (anders als Retell/ElevenLabs hat Twilios Webhook-URL keinen alternden Inhalt wie ein datumsabhängiger Prompt) — der manuelle „Erneut verbinden"-Button im Admin-Bereich genügt.

**Geprüft:** `npx tsc --noEmit`, `npx eslint .` (0 Fehler, dieselbe vorbestehende Warnung wie in Phase 1) und `npm run build` (alle 45 Routen inkl. `/api/voice/webhook/twilio/{voice,events}`) fehlerfrei; `npx vitest run` 38/38 grün (7 neue Fälle für `matchesCallFilters`). `get_advisors` (Supabase, `security`) zeigt keine neuen kritischen Befunde — die neuen `SECURITY DEFINER`-RPCs folgen demselben, bereits im Projekt etablierten Muster wie `toggle_salon_ai`/`update_own_booking_rules`.

---

## 4. Zusammenfassung: Reihenfolge & Abhängigkeiten

```
1 Terminologie → 2 Landingpage → 3 Branchenvorlagen ─┐
                                                      ├─→ 5 Onboarding
                                4 Self-Service-RLS ───┘
6 Status/Quellen erweitern ─→ 11 Ressourcen (setzt Phase 6 + 10 voraus)
7 Buchungsfragen (nach 4)     10 Standorte (nach 6, vor 11)
8 Anfragen (nach 4)           9 Kunden erweitern (unabhängig, jederzeit)
12 Meine Mia (unabhängig)     13 Rollen (unabhängig, aber sinnvoll nach 4)
14 Kalender-Feinschliff (nach 6/10/11, je nach gewünschten Filtern)
15 Demodaten (nach 3, sinnvoll begleitend zu jeder Fach-Phase)
16 Rename (optional, ganz am Ende oder separat beauftragt)
17 Statistiken/Benachrichtigungen (nach 8, 9)
```

Jede Phase soll einzeln committet, einzeln gebaut/getestet und erst danach gemergt werden — es wird ausdrücklich davon abgeraten, mehrere Phasen parallel im selben Arbeitsschritt umzusetzen (siehe Hinweis zu Beginn dieses Dokuments und der Produktspezifikation).
